# Проект assistant

## Что это
Проект `k/assistant` в Chatium с минимальными страницами и документацией. Общие стили: `styles.tsx` → `formControlStyles` (фокус, autofill), `customScrollbarStyles`, `mobileSafeAreaStyles` и `VIEWPORT_META_CONTENT` (viewport-fit, safe-area для body/модалок; шапка/футер дополнительно в `Header`/`AppFooter`). Выпадающие списки в модалках задач — `components/JnCrtSelect.vue` (панель через `Teleport` в `body`, `fixed`, непрозрачный фон); модалка задачи — класс `crt-form-panel` (сканлайны); дети модалки с `z-index` — `> *:not(.jn-crt-select)`. Главная с карточками быстрых разделов: «Мой журнал», «Мои задачи» и «Инструменты» — ссылки с переходом; «Диалоги», «PARA», «Финансы», «Сервисы», «Библиотека» — action-кнопки, которые запускают только `triggerGlitch` без навигации и без бизнес-логики; сетка карточек адаптирована под 8 элементов (desktop 4 колонки, tablet 2, mobile 1). Раздел «Инструменты» (`./web/tools/`, `ToolsPage.vue`) содержит одну карточку `Таймеры`, которая ведёт на `./web/timers`; страница `PomodoroPage.vue` подключает `components/pomodoro/PomodoroToolsWorkspace.vue` (вкладки `Помидор / Таймер / Секундомер`, Pomodoro UI, `FocusClockPane` для таймера/секундомера, сквозная строка `PomodoroToolStatsRow`). Для вкладок `Таймер/Секундомер` используется `components/pomodoro/FocusClockPane.vue` (селектор задачи, модалка настроек, фокус-чекбокс, запись в `POST /api/tools/focus-log`); нижняя статистика не дублируется внутри pane — три сквозные ячейки считаются в workspace (`lib/focus-clock-local-stats.ts` + Heap): «Сессий», «Работа», «Всего».

**Pomodoro (премиум-версия)**: `./web/timers/` — SSR для авторизованных: `pomodoro.lib.getState` → пропсы `initialPomodoroState` / `initialServerNowMs` в `PomodoroPage.vue` (не зависеть от первого клиентского `fetch`); `refresh` с повторными попытками и русскими текстами при сетевых ошибках; `getApiUrlForRoute` для абсолютных URL сохраняет query. `PomodoroPage.vue` — личный таймер с премиальным UX: динамическая тема (градиенты фона `theme-work`/`theme-rest`/`theme-long_rest`, смена CSS-переменных `--pomodoro-phase-color`), SVG-дуга с градиентами (`PomodoroTimerDial`, `linearGradient`, filter `glow`, pulse-анимация при `running`), моноширинный таймер, звук при смене фазы — пять пресетов Web Audio API (`lib/pomodoro-phase-sounds.ts`, поле `phaseChangeSound` 1–5 в Heap и в настройках), браузерные уведомления (Web Notifications API, запрос разрешения при mount), вибрация на мобильных (Vibration API), обновление `document.title` с таймером в неактивной вкладке; выбор задачи через `PomodoroTaskSelector` (API `api/tasks/in-progress`, dropdown с задачами статуса `in_progress`); поведение после завершения фазы настраивается отдельно для работы/отдыха/длинного отдыха в `PomodoroSettingsModal` с 3 режимами (`auto`/`pause`/`overtime`), совместимость с legacy `afterLongRest=stop` сохранена; статистика за день — `tasksCompletedToday` (инкремент при завершении work-фазы), визуализация в `.stat-card` (количество помидорок, время работы/отдыха); хранение состояния в Heap `tables/pomodoro-state.table.ts` (в т.ч. `autoStartRest`, `autoStartNextCycle`, `pauseAfter*`, `afterLongRest`, `phaseChangeSound`, `tasksCompletedToday`), а полный журнал сегментов запусков — в Heap `tables/pomodoro-launches.table.ts` (каждый старт/продолжение, фаза, задача, источник запуска, причина завершения, длительность сегмента); бизнес-логика `lib/pomodoro.lib.ts` (режимы завершения фаз, `runWithExclusiveLock`, автоматическое открытие/закрытие сегментов лога), репозитории `repos/pomodoro.repo.ts` и `repos/pomodoro-launches.repo.ts`, API `api/pomodoro/*` (`state/get`, `control`, `settings/save`, `assign-task`), `api/tasks/in-progress`. В `Header.vue` для `/web/timers` работает tool-widget в `header-clock`: при первом клике по часам выбирается `Помидор/Таймер/Секундомер`, после выбора доступны `Запуск/Пауза/Сброс`; pomodoro управляется через `pomodoroControlUrl`, timer использует текущие настройки длительности из `localStorage` (`assistant:focus-clock-settings:timer`), которые задаются на вкладке `Таймер` в `FocusClockPane.vue`. Для режимов «Помидор» и «Таймер» блок `header-clock` показывает слева-направо подложку прогресса (`.header-clock-progress`, насыщенный градиент на базе `var(--color-accent)` через `color-mix` + свечение), для секундомера и «часов» заливки нет. Кнопка `В помидор` из `JournalDayPane.vue` отправляет глобальное событие `assistant:focus-task-selected`: в шапке это событие запускает/продолжает выбранный инструмент, если он был остановлен, а при режиме `Часы` инициирует тройное красное мигание `header-clock` как UI-подсказку выбрать режим инструмента. В `TasksPage.vue` есть действие привязки задачи к Pomodoro (`api/pomodoro/assign-task`) и передача URL Pomodoro API в шапку страницы задач.

«Мой журнал» (`./web/journal/`, `JournalPage.vue`): порядок вкладок в меню: Инбокс, Блокнот, Задачи, День, Неделя, Месяц, Привычки; по умолчанию открывается Инбокс (без `?tab=`). «Блокнот» — `JournalNotebookPane.vue` (папки, категории, `NotebookNoteEditor`, Heap `journal-notes` + `api/journal/notes/*`); «Инбокс» — `JournalInboxPane.vue` (plain text, архивация, мультиселект как в блокноте и `NotebookBulkBar` без папок, Heap `inbox-notes` + `api/journal/inbox/*`). SSR `journalTabInitial`, `inboxNotesInitial`. Вкладка «Задачи» (`tasks`) — `JournalDayPane.vue`, задачи «В работе», модалка редактирования по клику на название (`api/tasks/items/update`), `daySortOrder`, API `api/tasks/items/reorder-day`, `release-day`; вкладка «День» (`day`) — `JournalDayInDevelopmentPane.vue` с 4 сегментами (`Ночь`, `Утро`, `День`, `Вечер`) и сохранением фиксации/разблокировки по `dayKey` (граница дня в 05:00, `api/journal/day/get|save`, `tables/journal-day-entries.table.ts`, `lib/journal-day-key.ts`); кнопки вкладок в левом меню всегда имеют видимую левую границу, активная вкладка выделяется акцентным цветом). «Управление задачами» (`./web/tasks/`, `TasksPage.vue`) — на десктопе три колонки (дерево, задачи, чат с AI: `components/tasks/TasksAiChatPanel.vue`, фиды `tasks-ai-chat-*`, сброс `tasks-ai-chat-reset`, история в `@app/feed`, вебсокет + polling; порядок сообщений в ленте нормализуется `shared/tasks-ai-chat-message-order.ts` — хронология старые→новые); на мобильном чат из FAB в шторке; иерархия клиент → проект → задача (у проекта и у задачи опционально «Детали» в Heap и UI; служебное поле `context` в Heap у проекта и задачи без выдачи в API; у задачи бывшее поле `description` заменено на `details`/`context`) (имя клиента в сайдбаре только подпись; красная подсветка клиента при выбранном его проекте — акцент слева через inset box-shadow; DnD порядка клиентов/проектов за рукоять, `api/tasks/clients/reorder`, `api/tasks/projects/reorder` (UX DnD не идеален, сценарий рабочий — см. architecture); в модалках кнопки `width: auto`), кнопка «Отметить» (переключение «К выполнению» ↔ «В работе»; при «В работе» — красная подсветка), чат с AI (системный промпт `getAiFormulateSystemPrompt` + `TASKS_AI_CHAT_JSON_APPENDIX`; перед completion в system подмешивается свежий блок проекта/задач из Heap, включая служебные `context` у проекта и задач (`buildTaskAiChatProjectContextBlock`) с явной меткой, что это служебный контекст; последнее сообщение пользователя отправляется отдельным user-блоком «ТЕКУЩЕЕ СООБЩЕНИЕ ПОЛЬЗОВАТЕЛЯ», чтобы модель не путала контекст с новым запросом; ответ модели — JSON с `reply`, `actions`, `summary`; рамка проекта — `update_project.context` (не дублировать список задач), по задачам — `update_task`/`create_task` с полным слитым `context` при необходимости; применение в Heap — `tasks-ai-formulate-apply.ts` из completion; в ленту пишется `reply`; отдельного роута `ai-formulate` нет; для reorder новых задач в том же ответе — плейсхолдеры `$new:0`, `$new:1`, …; настройки в админке), query `?client=` / `?project=`, Heap и API `api/tasks/` (tree, CRUD, reorder, reorder-day, release-day, tasks-ai-chat-*), DTO в `lib/tasks-types.ts`; обновление проекта по имени — без `clientId` в теле (иначе подставлялся бы клиент из выделения в сайдбаре). Heap: `Table.update(ctx, { id, ... })`, не три аргумента. Прелоадер: `shared/preloader.ts` — после `load` и `document.fonts.ready` — `boot-static-ready`, резерв 12 с / скрытие 15 с; `shared/bootUi.ts` — первая строка `// @shared`, шрифты (до 10 с), nextTick, rAF, `hideBootLoader`. AI-настройки: дефолты в `config/prompts.tsx` (системный промпт AI-формулирования / чата с задачами: массив действий (create_task с priority и декомпозицией шагов, update_task с title/priority, delete_task, reorder_tasks, update_project), опора на факты из контекста, без выдуманных taskId, ответ только JSON без Markdown-обёртки, без длинного тире U+2014 в текстах; **критическое правило разделения** — события с датами/действия → create_task, рамка проекта (не дублирование списка задач) → update_project, с примерами (few-shot); список моделей `AVAILABLE_AI_MODELS` — от худшей к лучшей для этой задачи, см. комментарий в файле), UI в `components/admin/AiSettings.vue` импортирует те же константы из `config/prompts`, сохранение через `api/settings/save`.

## Где искать детали
- Техническая информация: `README.md`
- Архитектура: `docs/architecture.md`
- API: `docs/api.md`
- Данные: `docs/data.md`
- Запуск: `docs/run.md`
- Решения: `docs/ADR/`
- История диалогов: `docs/LLM/`

## Актуализация 2026-03-26 (журнал — вкладки и привычки)
- `JournalPage.vue`: панели вкладок обёрнуты в `<KeepAlive :max="8">` внутри `<Transition name="journal-view">`; анимация переключения вкладок только по `opacity` (без `translateX`), чтобы не провоцировать артефакты отрисовки таблиц со `position: sticky` на фоне `overflow: hidden` у `.journal-panel`.
- `JournalHabitsPane.vue`: интервал опроса текущей недели (60 с) запускается через `startHabitsPoll` в `onMounted` и `onActivated`, останавливается в `onDeactivated` и `onUnmounted`, чтобы при кэше вкладок не оставлять фоновый `setInterval`.
- `JournalHabitsPane.vue`: `watch` на `journalHabitsInitial` применяет SSR только если `mondayKey` ещё пуст или совпадает с `initial` — иначе навигация по неделям не затирается снимком первого рендера.
- `repos/journal-habits.repo.ts` (`getHabitsWeekForUser`): для будущей недели Heap-запись не создаётся; при запросе возвращается пустой `rows`, устаревшая строка будущей недели в Heap удаляется. Для прошлой/текущей: если запись есть, но `rows` пустой после парсинга — подставляются названия с предыдущей недели и сохраняются. DTO включает `currentWeekMondayKey` для навигации (нельзя уйти вперёд за текущую неделю).

## Актуализация 2026-03-25 (журнал — вкладка «Привычки»)
- Вкладка `habits`: `components/journal/JournalHabitsPane.vue` — таблица привычек (строка = название + 7 чекбоксов Пн–Вс), навигация по неделям (вперёд не дальше текущей недели), подсказка про границу 05:00; на текущей неделе порядок строк меняется перетаскиванием за ручку (`fa-grip-vertical`) в первой колонке, сохранение порядка в том же `save`, что и остальные поля. Сервер: `lib/journal-habits-time.ts` (ключ недели с учётом `computeJournalDayKeyLocal`), `repos/journal-habits.repo.ts`, Heap `tables/journal-habits-week.table.ts`, `api/journal/habits/get.ts`, `api/journal/habits/save.ts`; в текущей неделе сервер принимает изменения чекбоксов только для «сегодняшней» колонки, прошлые недели — read-only; в Heap только прошлые и текущая недели.
- В `JournalPage.vue` при пайплайне ts+jsx избегать дженериков в исходнике (`defineProps`, `ref`, `Record`, аннотации вида `const x: Ref<T>`): угловые скобки после идентификатора парсятся как JSX и дают «Unexpected token, expected comma». Использовать `defineProps({ … })` с приведением типа, `ref(initial)` без дженерика (вывод типа с начального значения), явную карту компонентов вкладок вместо `Record<…>`.
- В шаблонах `.vue` (в т.ч. `JournalHabitsPane.vue`) не использовать TypeScript-синтаксис в inline-обработчиках (`as`, дженерики и т.п.): выносить в метод в `<script setup>`; для `input` удобно `instanceof HTMLInputElement` или приведение только в скрипте.

## Актуализация 2026-03-25 (журнал — вкладка «Месяц»)
- `components/journal/JournalMonthPane.vue`: детальный вид дня — секции как карточки с разделением шапки и контента (`jm-section-header` / `jm-section-body`), вертикальный стек с отступами; у блока завершённых задач — бейдж с количеством; элементы списка — карточки на `color-bg` с акцентной полосой и разделителем для меты клиент/проект. Календарь: сетка в рамке на вторичном фоне, ячейки с лёгкой подложкой при hover; время фокуса в ячейке — компактная подпись (`Nм` / `HчMм`), не точка; легенда под сеткой — суммы за месяц (число завершённых задач, суммарное время фокуса), подсказки поясняют отличие от показателей дня в ячейке. Шапка дневного вида с нижней границей для отделения от контента.

## Актуализация 2026-03-25 (архив заметок в журнале)
- В `JournalInboxPane.vue` и `JournalNotebookPane.vue` кнопка «Архив» переключает режим списка: по умолчанию видны только неархивные заметки, при нажатой кнопке — только архивные (`isArchived`). Пустое состояние в режиме архива: «В архиве пусто». В `NotebookFilterBar.vue` у кнопки архива — `aria-pressed`. `refreshList` в блокноте запрашивает `includeArchived=true`, чтобы после обновления по-прежнему были данные для обоих режимов.
- Массовое удаление в `NotebookBulkBar`: кнопка «Удалить» видна только если все выделенные заметки архивные (`showDelete`). В инбоксе у архивной карточки — «Удалить» рядом с «Вернуть». В блокноте `NotebookNoteCard` — колонка действий: «В архив» / «Вернуть» + «Удалить» для архивных.

## Актуализация 2026-03-25 (дата в блокноте)
- В `components/journal/NotebookNoteEditor.vue` поле даты работает в формате `dd/mm/yyyy` с авторасстановкой разделителей во время ввода: после 2 цифр (день) добавляется `/`, после 4 цифр (день+месяц) добавляется второй `/`; при сохранении значение валидируется и конвертируется в ISO `yyyy-mm-dd`.

## Актуализация 2026-03-25 (читабельность UI)
- По всему проекту усилена базовая типографика через глобальные стили: `baseHtmlStyles` (`html { font-size: 17px; }`, на узких экранах `16px`, повышенный `line-height`), плюс минимальный размер текста для полей и кнопок в `formControlStyles` (ориентир `16px`).
- Отдельно увеличены самые мелкие элементы в `Header.vue`, `AppFooter.vue` и `components/JnCrtSelect.vue`, чтобы убрать критически маленький текст в основных зонах взаимодействия.
- Выполнен комплексный масштаб по всем страницам и компонентам: локальные `font-size` в стилях Vue нормализованы до читаемого диапазона, чтобы выровнять восприятие текста между `Tasks`, `Journal`, `Pomodoro`, `Admin`, `Profile`, `Tests` и общими компонентами (включая редактор/тулбар блокнота и AI-панели).

## Актуализация 2026-03-25 (инструменты)
- В `web/tools` добавлены встроенные `Таймер` и `Секундомер` в стиле Pomodoro с кнопками `Старт/Пауза/Продолжить/Сброс` и чекбоксом `Фокус`.
- Для сохранения фокус-сегментов добавлен endpoint `POST /api/tools/focus-log` (`api/tools/focus-log.ts`), который пишет интервалы в `pomodoro-launches` как `phase=work` с источниками `tools_timer` и `tools_stopwatch`.
- При выключенном чекбоксе `Фокус` таймер и секундомер продолжают работать как обычные инструменты без записи в рабочее время.
- Таймер и секундомер вынесены из `ToolsPage` в отдельные страницы `web/timer` и `web/stopwatch` с единым компонентом `FocusClockPage.vue`; `ToolsPage` снова показывает только карточки перехода.
- Для `timer` в `FocusClockPage.vue` центральный визуальный блок переведён на `PomodoroTimerDial.vue`, чтобы отображение совпадало со стилем одного рабочего цикла Pomodoro (тот же SVG-dial).
- Для `stopwatch` в `FocusClockPage.vue` также используется `PomodoroTimerDial.vue` (единый визуальный стиль), но прогресс-кольцо зафиксировано без заполнения — только контур/стилизация циферблата.
- Стили `FocusClockPage` (phase bar, task selector, action panel, кнопки и stat-cards) приведены к тем же правилам, что и в `PomodoroPage` (`pomo-btn`-подобные состояния/эффекты, clip-path, scanlines, hover/focus).
- По следующему изменению архитектура упрощена: отдельные страницы `web/timer`/`web/stopwatch` и `pages/FocusClockPage.vue` удалены; `Таймер` и `Секундомер` встроены вкладками в `PomodoroPage`, общий код вынесен в `components/pomodoro/FocusClockPane.vue`.
- Для полной визуальной паритетности вкладок `Таймер/Секундомер` с вкладкой `Pomodoro` в `FocusClockPane.vue` убрано внутреннее сужение контейнера (pane растягивается на ширину `pomodoro-shell`), а верхняя панель приведена к той же схеме разметки (индикатор слева, центральный placeholder-блок, settings справа).
- Верхняя панель `FocusClockPane` переведена на CSS-grid (`1fr / auto / 1fr`), чтобы блок `cycle-dots` оставался строго по центру и не смещался из-за более длинного текста `phase-indicator` (например `СЕКУНДОМЕР`).
- Общие CRT-стили для трёх вкладок (`pomodoro-phase-bar`, `.pomodoro-actions`, `.pomo-btn`, `.pomodoro-tool-error`) — **второй блок `<style>` без `scoped` в `pages/PomodoroPage.vue`** (отдельные файлы `.css` и `import '*.css'` из `<script>` в бандлере Chatium не подключаются; не использовать только внешний `.css`); `FocusClockPane` использует те же классы, что Pomodoro (без отдельного префикса `clock-btn` / `clock-actions` для панели и кнопок).
- Для устранения «скачков» центральных точек в phase bar блок `cycle-dots` закреплён абсолютным центрированием (`left: 50%`, `translateX(-50%)`) и больше не зависит от длины текста слева/справа.
- В `FocusClockPane.vue` добавлено сохранение нижней статистики вкладок `Таймер/Секундомер` в `localStorage` с автоматическим восстановлением после перезагрузки страницы; хранение раздельное по режимам и по дневному ключу с границей суток в 05:00 (`computePomodoroStatsDayKeyLocal`); формат ключей и payload вынесен в `lib/focus-clock-local-stats.ts` (`// @shared`). Все три ячейки нижней строки счётчиков на **всех** вкладках `/web/timers` сквозные: сессии, рабочее+фокус-время, и «Всего» (`totalWorkSec` + `totalRestSec` Pomodoro + `totalSec` таймера и секундомера); общая разметка — `PomodoroToolStatsRow.vue`.
- В `FocusClockPane.vue` добавлен watcher по `props.mode` для корректного переключения persisted-статистики между вкладками `timer` и `stopwatch`: при смене режима предыдущие значения сначала сохраняются, затем для нового режима выполняется отдельное восстановление его счётчиков из `localStorage`.
- Для вкладок `Pomodoro` и `Таймер/Секундомер` унифицирован селектор задач: создан общий компонент `components/pomodoro/PomodoroTaskSelectDropdown.vue`, который используется в `PomodoroTaskSelector.vue` и `FocusClockPane.vue`.
- Для устранения рассинхрона выбора задачи между вкладками `Помидор / Таймер / Секундомер` общий `taskId` поднят в `pages/PomodoroPage.vue`: он хранится в `sharedSelectedTaskId`, синхронизируется с `state.currentTaskId` и persisted-ключом `assistant:focus-clock-selected-task:v1`, а `FocusClockPane.vue` получает/эмитит выбор через проп `selectedTaskId` и событие `taskSelected`.
- При выборе задачи во вкладках `Таймер/Секундомер` `PomodoroPage.vue` теперь выполняет `api/pomodoro/assign-task` и обновляет состояние Pomodoro, поэтому при переключении на вкладку `Помидор` сразу видна та же задача.
- В унифицированном селекторе добавлен текстовый поиск по первым символам сразу по трём полям: `task.title`, `projectName`, `clientName`.
- В списке вариантов селектора каждая задача показывается в 2 строки: заголовок задачи и мета `клиент / проект`; это снижает когнитивную нагрузку при длинных списках.
- `api/tasks/in-progress.ts` теперь возвращает для каждой задачи дополнительные поля `projectName` и `clientName` (вычисляются через маппинг `tasks -> projects -> clients`), чтобы UI-селекторы могли фильтровать и показывать иерархию без дополнительных запросов.

## Актуализация 2026-03-25 (архив задач)
- В `pages/TasksPage.vue` для колонки статуса добавлено inline-меню: клик по ячейке `Статус` открывает выпадающий список с быстрым переключением `К выполнению / В работе / Завершено / Отменено`.
- Смена статуса из меню сохраняется тем же endpoint `taskItemUpdateUrl` и сразу отражается в таблице без перезагрузки страницы.
- В шапке панели задач добавлен переключатель `Архив/Активные`.
- В режиме `Активные` показываются только задачи, у которых статус не архивный; статусы `done` и `cancelled` считаются архивными.
- В режиме `Архив` показываются только задачи со статусами `Завершено` и `Отменено`.
- Hotfix сборки: TypeScript-cast в template-обработчике (`option.value as TaskItemDto['status']`) заменён на вызов helper-функции `updateTaskStatusByValue` из `<script setup>`, чтобы убрать UGC SyntaxError `Unexpected token`.

## Критические правила
- Не хардкодить URL, использовать `config/routes.tsx`.
- Документацию обновлять при любых изменениях структуры/логики.
- Структуру и стили левого меню журнала держать в `components/journal/JournalNav.vue`; `JournalPage.vue` не должен содержать прямую разметку `nav.journal-nav`.
- В `JournalPage.vue` для вкладки `tasks` допускается только переход по переданному `tasksPageUrl` (кнопка `Все задачи`), без хардкода путей.
- Динамические кнопки в `JournalPage.vue` (`Новая заметка` и `Все задачи`) должны рендериться после `journal-nav-divider`; для action-кнопок использовать кастомный `:focus-visible` в цветах темы (без системной жёлтой обводки браузера).
- Для кнопок вкладок журнала (`.journal-nav-btn`) также использовать кастомный `:focus-visible` в цветах темы и не оставлять браузерный системный outline.
- Порядок вкладок в левом меню журнала фиксированный: `tasks -> day -> week -> month -> habits -> notebook -> inbox` (по лейблам: `Задачи, День, Неделя, Месяц, Привычки, Блокнот, Инбокс`).
- Для Pomodoro любые мутации состояния выполнять только через `lib/pomodoro.lib.ts` под `runWithExclusiveLock`; запись в `tables/pomodoro-state` напрямую из API/страниц не делать.
- Модули `lib/pomodoro-types.ts`, `lib/pomodoro-phase-sounds.ts` и `lib/pomodoro-stats-day.ts`, импортируемые из Vue, должны начинаться с `// @shared` (иначе ошибка сборки про отсутствие shared file mark).
- Для интеграции Pomodoro в шапке страницы передавать `pomodoroStateGetUrl` и `pomodoroControlUrl` в `Header.vue`; без этих URL таймер/кнопка не активируются; запросы дополнять `statsDayKey` (см. `lib/pomodoro-stats-day.ts`) для корректного сброса дневной статистики в 05:00 локально.
- Чтобы включить новый виджет `header-clock` только на нужной странице, передавать в `Header.vue` проп `enableToolClockWidget`; в проекте он включён на `PomodoroPage.vue`, на остальных страницах шапка остаётся в режиме обычных часов.
- Внутреннее состояние виджета (`выбранный режим`, `status` и тайминги локальных `timer/stopwatch`) сохраняется в `localStorage` (`assistant:header-clock-widget:v1`), поэтому после reload режим и время продолжают корректно отображаться; параллельный запуск запрещён — при старте одного инструмента остальные ставятся на паузу.
- При первом рендере `Header.vue` автоматически переключается на активный инструмент, если обнаружен `running`/`awaiting_continue` Pomodoro или `running` локальный timer/stopwatch; если активного инструмента нет, остаётся выбранный пользователем режим. Логика применяется на всех страницах, где используется `Header`.
- Выбор режима в `header-clock` оформлен как выпадающий вертикальный picker под часами (`Часы/Помидор/Таймер/Секундомер`), чтобы не ломать header по ширине на мобильных и узких экранах; сохранены иконки, активное состояние (`header-tool-picker-btn--active`), `aria-pressed`, `focus-visible`, закрытие по `Escape` и по клику вне picker-блока.
- Подложка `header-tool-picker` должна быть непрозрачной (`var(--color-bg-secondary)`), чтобы элементы контента не просвечивали за выпадающим меню.
- Для сценария «выбрать задачу и сразу запустить фокус» использовать событие `assistant:focus-task-selected` (`window.dispatchEvent`): его отправляет `JournalDayPane.vue` после успешного `assign-task`, а `Header.vue` принимает и применяет правило: если режим инструмента уже выбран (`pomodoro|timer|stopwatch`) и инструмент остановлен — запускает/продолжает; если уже запущен — не перезапускает; если выбран режим `clock` — трижды мигает красным `header-clock`.
- Чтобы не было рассинхрона между шапкой и `/web/timers`, `components/pomodoro/FocusClockPane.vue` должен синхронизировать runtime-состояние из persisted-state шапки (`assistant:header-clock-widget:v1`) при mount и при смене вкладки режима (`timer/stopwatch`): иначе возможно состояние «в шапке тикает, на странице стоит».
- Для сквозной передачи выбранной задачи между журналом, шапкой и страницей таймеров используется persisted-ключ `assistant:focus-clock-selected-task:v1`: `Header.vue` пишет туда `taskId` при событии `assistant:focus-task-selected`, `FocusClockPane.vue` читает этот `taskId` при mount и синхронизирует его обратно при ручном выборе в селекторе.
- Событие `taskAssigned` из `PomodoroTaskSelector.vue` должно передавать `taskId` в `PomodoroPage.vue`, чтобы общий selected-task state оставался единым для всех трёх вкладок.
- Правило reset для `timer/stopwatch`: кнопка `Сброс` в `Header.vue` и `FocusClockPane.vue` обязана очищать persisted-ключ выбранной задачи `assistant:focus-clock-selected-task:v1`; для live-синхронизации селектора задачи между шапкой и страницей используется событие `assistant:focus-task-cleared`.
- Для live-синхронизации `Пауза/Запуск` между `Header.vue` и `FocusClockPane.vue` используется событие `assistant:header-clock-state-changed`: шапка отправляет его после `persistHeaderWidgetState()`, страница таймеров принимает и делает `syncClockStateFromHeaderWidget()`.
- Для полной двусторонней синхронизации `FocusClockPane.vue` также публикует состояние обратно в `assistant:header-clock-widget:v1` (и отправляет `assistant:header-clock-state-changed`) при `start/pause/resume/reset` и авто-остановке таймера; это устраняет кейс «остановил на странице, а в шапке продолжает идти».
- Для корректной реакции шапки на изменения со страницы `Header.vue` должен слушать `assistant:header-clock-state-changed` и вызывать `restoreHeaderWidgetState()`; без этого события от `FocusClockPane.vue` игнорируются в текущей вкладке.

## Актуализация 2026-03-24 (WYSIWYG-редактор блокнота)
- В `NotebookNoteEditor` textarea заменён на полноценный WYSIWYG-редактор (`WysiwygEditor.vue` + `WysiwygToolbar.vue`): contenteditable без внешних зависимостей, контент — HTML-строка.
- В верхней панели `NotebookNoteEditor` добавлена кнопка `Экспорт` с выпадающим меню: `Скачать TXT (без разметки)`, `Скачать MD (с разметкой)` и `Печать в PDF…` (новое окно печати с форматированным шаблоном).
- Для `Печать в PDF…` в `window.open` нельзя использовать `noopener,noreferrer`: в ряде браузеров вкладка открывается, но ссылка на окно возвращается как `null`, из-за чего показывается ложная ошибка про pop-up и остаётся `blank`; используется открытие с размером окна без этих флагов.
- В печатном шаблоне PDF удалена мета-строка `Экспортировано: ...` (дата/время): в итоговом документе остаются заголовок и содержимое заметки.
- Возможности тулбара: жирный/курсив/подчёркнутый/зачёркнутый, цвет текста (16 пресетов + custom color), тип блока (H1/H2/H3/абзац/обычный текст), маркированный/нумерованный списки (с indent/outdent для вложенности), выравнивание (лево/центр/право/ширина), ссылки (диалог с href + target), таблицы (grid-picker для размера + добавление/удаление строк/столбцов через контекстное меню), HR, inline code (`<code>`), блок кода (`<pre data-language><code>` с подсветкой синтаксиса через highlight.js CDN), цитата (`<blockquote>`), undo/redo, очистка форматирования, **вставка медиафайлов**.
- **Блок кода с подсветкой синтаксиса**: кнопка «Блок кода» вставляет `<pre data-language="plaintext"><code class="hljs">` в редактор. При фокусе внутри блока кода над ним слева (левый верхний угол над `pre`) появляется floating-пикер языка (25 языков: JavaScript, TypeScript, Python, HTML, CSS, JSON, Bash, SQL, PHP, Java, C++, C#, Go, Rust, Ruby, YAML, XML, Markdown, Swift, Kotlin, SCSS, Less, Dockerfile, Nginx, plaintext). Подсветка синтаксиса реализована через **highlight.js v11.11.1 с CDN cdnjs.cloudflare.com** (тема `atom-one-dark`): загрузка ленивая (JS + CSS грузятся при первой необходимости); подсветка применяется при выборе языка, при уходе фокуса из блока, при вводе с дебаунсом 700ms (с сохранением позиции курсора). Язык сохраняется в атрибуте `data-language` на `<pre>` и хранится в HTML-строке заметки. При вставке Markdown с fenced code blocks (` ```js `) язык парсится и применяется автоматически. Экспорт в Markdown включает язык: `` ```javascript ``. Визуально блок выделяется акцентной левой рамкой, при фокусе — свечение.
- **Медиа-вложения** (`WysiwygToolbar.vue` → `WysiwygEditor.vue`): кнопка «Добавить файл» (скрепка) с поддержкой изображений (`<img>`), видео (`<video controls>`), PDF (`<iframe>` + ссылка), документов (карточка файла с иконкой расширения и ссылкой на скачивание). Валидация размера файла (до 50 МБ), индикатор загрузки (спиннер).
- **Управление медиа-элементами**: при клике на вставленное медиа появляется bubble menu с кнопками действий (CRT-стиль, `clip-path`, scanlines overlay). Для изображений — выравнивание (лево/центр/право) и resize-handle для изменения размера (drag). Для всех типов — кнопка «Скачать» и «Удалить». Выделенный элемент подсвечивается рамкой (`box-shadow` с акцентным цветом). Меню позиционируется через `Teleport` в `body` с анимацией `wy-bubble-fade`.
- Обратная совместимость: при загрузке plain text автоматически конвертируется в `<p>`.
- Вставка plain text в `WysiwygEditor` обрабатывается через `@paste`: строки нормализуются и превращаются в последовательность `<p>...</p>`, поэтому пустые строки из буфера (`\n\n`) сохраняются и не схлопываются в одну.
- Вставка из буфера в `WysiwygEditor` работает в режиме smart-paste: если доступен `text/html`, вставляется готовая разметка; если есть только `text/plain`, при признаках Markdown выполняется базовая конвертация в HTML (headers/lists/blockquote/code/hr + inline `**`/`*`/`` ` ``/links), иначе используется построчная вставка как обычного текста.
- В Markdown-ветке smart-paste пустые строки не теряются: каждая пустая строка из буфера превращается в `<p><br></p>`, чтобы визуально сохранялись двойные переносы между блоками.
- Вставка HTML при paste выполняется через прямую DOM-вставку (`insertHtmlAtCursor` на Range API) вместо `document.execCommand('insertHTML')`, чтобы браузер не нормализовал/удалял пустые абзацы между блочными элементами. Пустые строки помечены классом `wy-blank-line` с CSS `padding: 0.4rem 0; height: 0; overflow: hidden; font-size: 0; line-height: 0` — padding в `rem` не коллапсируется и гарантирует визуальное пространство в `contenteditable`.
- Внутри блока кода (`pre[data-language]`) paste не использует `insertHtmlAtCursor`: вставляется только plain text в `<code>` через Range (`clipboardPlainTextForCodeBlock`, `insertPlainTextIntoCodeBlockAtSelection`, при необходимости `ensureRangeInsidePreCode`), чтобы текст не попадал в DOM сразу *под* `<pre>`.
- В `WysiwygEditor` есть переключатель `WYSIWYG / HTML`: в `HTML` показывается исходный HTML заметки в `textarea` для диагностики и ручной правки; изменения синхронно сохраняются в `modelValue`; при переключении обратно в `WYSIWYG` содержимое прогоняется через `ensureHtml` и рендерится в визуальном редакторе.
- При переключении `WYSIWYG -> HTML` source-строка дополнительно нормализуется функцией `normalizeSourceHtml`: удаляются только ведущие `\r/\n` в начале, чтобы в `textarea` не появлялась пустая первая строка, если браузер отдал `innerHTML` с техническим переносом.
- В `WysiwygEditor` усилена синхронизация при переключении `WYSIWYG / HTML`: введён буфер `lastKnownHtml` и fallback-приоритеты источников (`sourceHtml` -> `modelValue` -> `lastKnownHtml`), чтобы при быстрых переключениях не терялось содержимое из-за промежуточной пустой строки.
- В `WysiwygEditor` учтён жизненный цикл `v-if` для `contenteditable`: при возврате из `HTML` в `WYSIWYG` применение `innerHTML` делается в `nextTick`, когда `editorRef` уже заново создан в DOM; иначе контент мог «пропадать» из-за ранней попытки записи.
- При paste приоритет источника динамический: если `text/plain` содержит Markdown-признаки или двойные переносы (`\n\n`), используется plain-text конвертация (чтобы сохранить структуру и пустые строки), даже если в буфере есть `text/html`; в остальных случаях сохраняется вставка из `text/html`.
- Для технических пустых строк используется класс `wy-blank-line`, но теперь он «самоочищается»: если пользователь вводит в такой абзац текст, `normalizeBlankLineParagraphs` снимает класс, и текст сразу виден в визуальном режиме.
- Язык блока кода: последний выбор в floating-пикере сохраняется в `localStorage` (`assistant-journal-preferred-code-lang`); им же задаётся язык новых блоков с панели и fenced-блоков Markdown без языка при вставке.
- Глобальные стили контента редактора (заголовки, списки, таблицы, код, цитаты, ссылки, HR, медиа) определены в `WysiwygEditor.vue` через не-scoped `<style>`.

## Актуализация 2026-03-24 (блокнот)
- Блокнот полностью переписан: вместо модального редактора — split-view с боковой панелью папок (`NotebookFolderSidebar`) и списком заметок; полностраничный редактор (`NotebookNoteEditor`); фильтр-бар (`NotebookFilterBar` — поиск, фильтр по категории, переключатель архива); мультиселект с панелью массовых действий (`NotebookBulkBar` — перемещение, архивация, удаление, назначение категории). Карточки заметок (`NotebookNoteCard`): выделение кликом по телу карточки, открытие редактора — клик по заголовку; метки категории, превью. Инлайн-управление категориями (создание, редактирование цвета/имени, удаление).
- Новые Heap-таблицы: `notebook-categories` (userId, name, color, sortOrder) и `notebook-folders` (userId, name, color, sortOrder, isArchived).
- Расширена таблица `journal-notes`: добавлены folderId, categoryIds (JSON string), linkedTaskId, linkedProjectId, linkedClientId, noteDate, isArchived, sortOrder.
- Новые репозитории: `notebook-categories.repo.ts`, `notebook-folders.repo.ts`; расширен `journal-notes.repo.ts` (reorderForUser, bulkArchiveForUser, bulkMoveToFolderForUser, bulkDeleteForUser, bulkSetCategoryForUser, archiveByFolderForUser, clearFolderIdForUser, removeCategoryFromAllNotes).
- Новые API: `api/journal/categories/*` (list/create/update/delete), `api/journal/folders/*` (create/update/delete/reorder/archive), `api/journal/notes/reorder|archive|move|bulk`. Обновлены notes/list (теперь notes+folders+categories), notes/get (расширенное DTO), notes/create и notes/update (принимают новые поля).
- `JournalNotebookPane.vue` — полный рерайт, `JournalPage.vue` — расширены пропсы для новых URL и SSR-данных, убрана «Новая заметка» из навигации, добавлен `reloadNotebookData`. `web/journal/index.tsx` — импорты/пропсы для всех новых маршрутов.

## Актуализация 2026-03-24
- Вкладка «Месяц» в журнале (`components/journal/JournalMonthPane.vue`): два режима — **календарь** (навигация по месяцам, бейджи завершённых задач, компактные подписи времени фокуса Pomodoro) и **детальный день** (клик по ячейке → заголовок с датой/днём недели, навигация ←/→ по дням, список завершённых задач по `updatedAt`, время фокуса + заглушка аналитики, план на день из «Недели», записи из «Дня»). Новый API `api/journal/month/data.ts` (`GET ?year=&month=`), новые функции `repos/tasks.repo.ts::getCompletedTasksSummaryForMonth` и `repos/pomodoro-launches.repo.ts::getWorkFocusByDayForMonth`. Пропс `journalMonthDataUrl` прокинут через `web/journal/index.tsx` → `JournalPage.vue` → `JournalMonthPane.vue`.
- В детальном режиме вкладки «Месяц» включено редактирование на месте: блок «План на день (из недели)» сохраняется через `api/journal/week/save` и имеет фиксацию `Зафиксировать/Редактировать`; блоки `Ночь/Утро/День/Вечер` сохраняются через `api/journal/day/save` с отдельной фиксацией каждого сегмента. Поля доступны даже для пустых значений.

## Актуализация 2026-03-23
- На вкладке `week` в `components/journal/JournalWeekPane.vue` действие возврата после пролистывания недель перенесено из верхнего ряда в отдельную подстроку: `К текущей неделе` рядом с `Начало недели: ...`; верхний ряд снова симметричен (`← Пред.` / `Неделя #N` / `След. →`), что сохраняет центрирование заголовка.
- На вкладке `week` в `components/journal/JournalWeekPane.vue` в блоке `journal-week-controls` добавлена кнопка `Текущая` для быстрого возврата после пролистывания недель; кнопка дизейблится при уже открытой текущей неделе и во время `loadingSwitch`.
- Для точного выравнивания weekly-summary с карточками дней на вкладке `week` в `components/journal/JournalWeekPane.vue` summary-шапка переведена на ту же компоновку, что у дневной карточки: добавлены `journal-week-summary-caption` и скрытый `journal-week-summary-date-spacer` (на базе `journal-week-day-date`), а `journal-week-summary-head` выровнен по `align-items: flex-start`; это убирает остаточный «плывущий» сдвиг textarea.
- На вкладке `week` в `components/journal/JournalWeekPane.vue` у weekly-summary синхронизирован вертикальный отступ шапки с дневными карточками: `journal-week-summary-head` получил увеличенный `margin-bottom`, чтобы textarea `План на неделю целиком…` начиналась на том же уровне, что поля Пн–Вс.
- Выполнен обязательный прогон `npm run lint:types:assistant` с исправлением типизации в `k/assistant`: нормализация `phaseChangeSound` в API сохранения Pomodoro, подавление TS-ошибки внешнего runtime-модуля `@start/sdk` через `// @ts-ignore`, выравнивание уровней для `ctx.account.log` в `lib/logger.lib.ts`, замена `TaskProjects.T/TaskItems.T` на `TaskProjectsRow/TaskItemsRow` в `repos/tasks.repo.ts`, удаление импорта `nextTick` из `shared/bootUi.ts` и уточнение generic-constraint в `shared/tasks-ai-chat-message-order.ts`.
- Для системного запуска линтинга TypeScript проекта `k/assistant` в корневой `package.json` используется скрипт `lint:types:assistant` (`npx -p typescript@5 --yes tsc --noEmit -p k/assistant/tsconfig.json`); требование обязательного запуска перед финальной проверкой добавлено в `inner/docs/020-testing.md`.
- Для устранения конфликта TypeScript 5/6 в `k/assistant/tsconfig.json` удалён deprecated `baseUrl` и убран `ignoreDeprecations`; alias через `paths` сохранён, проверка проходит как в TS6 (`npx -p typescript --yes tsc --noEmit ...`), так и в проектном скрипте на TS5.
- На вкладке `week` в журнале добавлен отдельный weekly-summary блок `Неделя #N` (многострочное поле + `Зафиксировать`/`Редактировать`) для общего плана недели; дневные карточки Пн–Вс остаются ниже.
- Для weekly-summary добавлены: таблица `tables/journal-week-summary.table.ts`, API `api/journal/week/save-summary.ts`, прокидка URL в `web/journal/index.tsx`/`JournalPage.vue`; `repos/journal-week-entries.repo.ts` возвращает `week.summary` и сохраняет summary через `saveWeekSummaryForUser`.
- В журнале на вкладке `week` вместо заглушки реализован недельный план по дням (`Понедельник`…`Воскресенье`) с многострочным полем, датой и кнопкой `Зафиксировать`/`Редактировать` для каждого дня; сверху добавлены `Неделя #N` и переключение `предыдущая/следующая`.
- Для недельного плана добавлены `api/journal/week/get.ts` и `api/journal/week/save.ts`, репозиторий `repos/journal-week-entries.repo.ts`, таблица `tables/journal-week-entries.table.ts`, а также `lib/journal-week-key.ts` (локальный ключ понедельника недели, сдвиг недели, список дат и номер недели).
- `web/journal/index.tsx` теперь делает SSR-предзагрузку текущей недели и передаёт в `JournalPage.vue` URL/initial-данные для вкладки `week`.
- В `styles.tsx` (`formControlStyles`) добавлено глобальное low-specificity правило для текстовых контролов (`input` кроме checkbox/radio/range/color/file, `textarea`, `select`): на `:focus/:focus-visible` отключается системный `outline` и browser `box-shadow`, чтобы по всему проекту не появлялась нативная жёлтая обводка; компонентные фокус-стили остаются приоритетными.
- В `components/JnCrtSelect.vue` селект больше не зависит от внешнего класса `jn-input`: у `.jn-crt-select__trigger` заданы собственные базовые стили поля (`padding`, `border`, `background`, `color`, `font-size`, фокус-обводка), что устраняет «сломанный» вид в scoped-модалке `JournalDayPane`; у `.jn-crt-select__value` сохранены `display: block` + наследование `line-height`/`letter-spacing` для стабильного рендера текста.
- Для аналитики Pomodoro добавлена таблица `tables/pomodoro-launches.table.ts` и репозиторий `repos/pomodoro-launches.repo.ts`; в `lib/pomodoro.lib.ts` лог сегментов обновляется на всех переходах состояния: `start/resume/pause/stop`, автопереходы фаз, смена задачи во время `running`, а также recovery-закрытие «зависшего» сегмента при несогласованном состоянии.
- Для `web/tools/index.tsx` и `web/timers/index.tsx` обязательно задавать базовые CSS-переменные темы (`:root`), фон `html/body`, шрифт и подключение `customScrollbarStyles`/`mobileSafeAreaStyles`, иначе страницы визуально выглядят как «белый экран».
- В `ToolsPage.vue` и `PomodoroPage.vue` `app-layout` должен иметь `min-height: 100vh`, чтобы `content-wrapper` с `flex-1` не схлопывался.
- `PomodoroPage.vue` должен показывать fallback-состояние загрузки до получения `state` (не оставлять пустой `main` при ожидании API).
- Для API `api/pomodoro/state/get.ts` и `api/pomodoro/settings/save.ts` корректный импорт `pomodoro.lib` — `../../../lib/pomodoro.lib`; путь `../../../../lib/pomodoro.lib` приводит к runtime `HTTP 500` на странице `/web/timers`.
- **Импорт таблиц Heap**: используй `import TableName from '../tables/table-name.table'` (default import), а не `import { TableName } from ...` (named import). Неправильный импорт приводит к ошибке «Не удалось получить состояние» в runtime.
- Контроль Pomodoro: `start` — рабочая фаза и `cyclesCompleted=0` (на странице — только из `stopped`); `resume` — из `paused` или `awaiting_continue`; `pause` — из `running`; `skip` — пропуск фазы (на странице — только при `running`); `reset` — полная остановка с обнулением циклов (`stopped`, полный work); при пропуске во время отсчёта в суммы и задачу не доначисляется остаток фазы; `stop` — в API сохранён, в UI страницы Pomodoro не выводится. После естественного окончания фазы по таймеру статус `awaiting_continue`, `phaseEndsAtMs` — момент окончания фазы (начало секундомера овертайма на клиенте).
- Текущая UX-семантика `PomodoroPage`: compact layout, основной таймер отображается как SVG-дуга (`PomodoroTimerDial`), настройки вынесены в отдельную модалку (`PomodoroSettingsModal`), inline-поля на странице отсутствуют; черновик настроек в модалке синхронизируется с `modelValue` только при открытии (не при каждом poll), иначе числовые поля сбрасывались бы к сохранённым значениям.
- Для устойчивости UI Pomodoro использовать защиту от гонок: блокировка повторного control-действия (`actionPending`), приоритет более свежего `serverNowMs`, принудительный `refresh` после каждого control, обновление состояния при `focus`/`visibilitychange`.
- **Премиум-функционал Pomodoro**: динамическая тема через классы `.theme-work`/`.theme-rest`/`.theme-long_rest` на `.app-layout` (градиенты фона, переменная `--pomodoro-phase-color`); SVG-градиенты на дуге (`linearGradient` в `PomodoroTimerDial`), мягкое статичное свечение `filter: url(#glow)` без пульсации в `running` (чтобы не мигал белый blur-фон); звук при смене фазы — пять пресетов (`phaseChangeSound` 1–5, `lib/pomodoro-phase-sounds.ts`, выбор в модалке настроек); браузерные уведомления (Web Notifications API, запрос разрешения); вибрация (Vibration API, паттерн `[200, 100, 200]`); обновление `document.title` с таймером и эмодзи фазы в фоновой вкладке; выбор задачи через `PomodoroTaskSelector` (`api/tasks/in-progress`); после завершения каждого этапа применяется выбранный режим: `auto` (автопереход), `pause` (переключить фазу и ждать ручного resume), `overtime` (`awaiting_continue` + отрицательный таймер до ручного переключения); статистика за день (`.stat-card`, `tasksCompletedToday` инкрементируется при завершении work-фазы по таймеру или при пропуске работы); Vue `transition` для смены фаз.
- **Тюнинг мигания таймера**: в `PomodoroTimerDial.vue` мигание в `status=paused` сделано едва заметным: `paused-blink` и `time-paused-blink` замедлены до `2.6s`, минимум opacity повышен (дуга `0.5`, время `0.88`), `dial-scanline-flicker` ослаблен (диапазон `0.36..0.42`).
- **Устранение белого blur-пульса**: в `PomodoroTimerDial.vue` удалена анимация `progress-pulse` и связанный `drop-shadow` для `status=running`; интенсивность SVG-фильтров снижена (`glow: 3`, `glow-strong: 4`), чтобы убрать заметное мигание ореола под дугой.
- **CRT-стиль Pomodoro**: `web/timers/index.tsx` включает полный набор CRT-слоёв (geometric-bg с виньеткой и SVG-сеткой, scanlines body::after, bezel body::before, crt-power-on анимация), идентичный `index.tsx` главной. Все элементы UI используют `clip-path` polygon для пиксельных углов (2px–6px), `repeating-linear-gradient` для scanlines overlay, `text-shadow` с RGB chromatic aberration. Phase bar (`.pomodoro-phase-bar`) — индикатор фазы с пульсирующей точкой, cycle dots прогресса, кнопка настроек. Кнопки `.pomo-btn` с вариантами `--primary`/`--secondary`/`--ghost`/`--danger`: scanlines `::before`, underline `::after` на hover. `PomodoroTimerDial` — 60 tick-марок, `glow-strong` filter, blink-анимация при паузе. `PomodoroSettingsModal` — секции с иконками, grid числовых полей, кастомные чекбоксы/select. `PomodoroTaskSelector` — scanlines overlay на toggle и dropdown.
- **Кнопки управления Pomodoro**: под таймером `.pomodoro-actions` → панель `.pomodoro-actions__panel`, класс раскладки `pomodoroActionPanelLayout`: `--n1` (только «Старт» при `stopped`, по центру), `--n2` (две колонки: при `running` — «Пауза» + «Пропустить»; при `paused` / `awaiting_continue` — «Продолжить» + «Сбросить`, действие `reset`). Классы `.pomo-btn`: `--primary`/`--secondary`/`--ghost`/`--danger`; подписи uppercase; `:focus-visible`; `actionPending`; safe-area в `.pomodoro-shell`.
- **Формат времени Pomodoro на клиенте**: `lib/pomodoro-types.ts` → `formatPomodoroSecondsDisplay` — до 59:59 — `MM:SS`, с 1 ч — `H:MM:SS` (статистика `.stat-cell`, таймер, `document.title`, таймер в `Header.vue`).
