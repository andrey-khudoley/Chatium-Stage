/**
 * Generates a CSRF token with a given name meant for granular CSRF protection of dynamic SPA pages/fragments.
 * Current request must have a valid x-csrf-token header,
 *  containing page CSRF token with the same name as the given contextPageTokenName.
 */
export declare function generateDynamicCsrfToken(
  _ctx: RichUgcCtx,
  name: string,
  contextPageTokenName: string,
): Promise<string>
/**
 * Verifies a dynamic CSRF token generated with generateDynamicCsrfToken.
 */
export declare function verifyDynamicCsrfToken(
  _ctx: RichUgcCtx,
  token: string,
  validationProps: CsrfTokenValidationProps,
): Promise<number>
export declare type CsrfTokenValidationProps = {
  name: string
  referers?: string[]
  expiresInSeconds?: number
}

export {}
