---
title: 'Т-Банк Эквайринг — Webhooks и статусы платежа'
type: reference
tags:
  - topic/t-bank
  - topic/payments
  - topic/webhooks
created: 2026-05-08
---

# Т-Банк Эквайринг — Webhooks и статусы платежа

## Как настроить получение уведомлений

**Вариант 1 — Глобально через ЛК:**  
ЛК интернет-эквайринга → Магазины → Терминалы → Настроить → указать URL для HTTP(S)-уведомлений.

**Вариант 2 — Для конкретного платежа через Init:**  
Передать параметр `NotificationURL` в запросе метода `Init`. Это **переопределяет** глобальную настройку для данного платежа.

```json
{
  "NotificationURL": "https://example.com/webhook/tbank",
  ...
}
```

> Рекомендуется: `NotificationURL` в Init — это самый гибкий вариант для CRM-систем, где каждый заказ имеет свой контекст.

---

## Формат webhook-уведомления

Т-Банк отправляет **POST-запрос** с `Content-Type: application/x-www-form-urlencoded` на указанный URL.

### Поля уведомления

| Поле          | Тип     | Описание                                            |
| ------------- | ------- | --------------------------------------------------- |
| `TerminalKey` | string  | Ключ терминала                                      |
| `OrderId`     | string  | ID заказа мерчанта (из Init)                        |
| `Success`     | boolean | `true` при успешном статусе                         |
| `Status`      | string  | Текущий статус платежа                              |
| `PaymentId`   | integer | ID платежа в системе банка                          |
| `ErrorCode`   | string  | `0` при отсутствии ошибок                           |
| `Amount`      | integer | Сумма в копейках                                    |
| `Pan`         | string  | Маскированный номер карты, напр. `430000******0777` |
| `ExpDate`     | string  | Срок действия карты, формат `MMYY`                  |
| `CardId`      | integer | ID привязанной карты (если есть)                    |
| `RebillId`    | string  | ID рекуррентного платежа (если есть)                |
| `Token`       | string  | Подпись уведомления для верификации                 |

**Пример тела уведомления (URL-encoded):**

```
TerminalKey=TinkoffBankTest&OrderId=order_42&Success=true&Status=CONFIRMED&PaymentId=13660038&ErrorCode=0&Amount=149900&Pan=430000******0777&Token=<hash>
```

---

## Верификация подписи уведомления

Необходимо проверить, что уведомление действительно пришло от Т-Банка.

**Алгоритм (аналогичен генерации токена при запросах):**

1. Взять все поля уведомления (кроме `Token`).
2. Добавить пару `Password` → пароль терминала из ЛК.
3. Отсортировать по ключу алфавитно.
4. Конкатенировать только значения в одну строку.
5. Вычислить SHA-256.
6. Сравнить с полученным `Token`.

**Пример верификации (PHP):**

```php
function verifyWebhook(array $params, string $password): bool {
    $receivedToken = $params['Token'];
    unset($params['Token']);
    $params['Password'] = $password;
    ksort($params);
    $str = implode('', array_values($params));
    $calculatedToken = hash('sha256', $str);
    return hash_equals($calculatedToken, $receivedToken);
}
```

---

## Ответ на webhook

После получения и обработки уведомления ваш сервер **обязан** вернуть:

```
HTTP 200 OK
Body: OK
```

Без HTML-тегов, только английские заглавные буквы `OK`.

**Что происходит при не-200 или отсутствии `OK`:**

- Т-Банк считает доставку неуспешной.
- Повторяет отправку **раз в час в течение 24 часов**.
- Затем повторяет **раз в сутки в течение месяца**.

---

## Жизненный цикл статусов платежа

```
Init → NEW
         ↓
    FORM_SHOWED (покупатель открыл форму)
         ↓
    AUTHORIZING (резервирование средств)
         ↓
    3DS_CHECKING (если требуется 3-D Secure)
         ↓
    3DS_CHECKED
         ↓
    AUTHORIZED (средства зарезервированы)
         ↓
    CONFIRMING
         ↓
    CONFIRMED ✅ (деньги списаны — ФИНАЛЬНЫЙ УСПЕШНЫЙ СТАТУС)
```

Альтернативные ветки:

```
AUTHORIZED → REVERSING → REVERSED (отмена до списания)
CONFIRMED  → REFUNDING → REFUNDED / PARTIAL_REFUNDED (возврат после списания)
```

Ошибочные статусы:

```
AUTHORIZING → AUTH_FAIL (ошибка авторизации)
AUTHORIZING → REJECTED (отклонён банком)
любой → DEADLINE_EXPIRED (истёк срок ссылки)
любой → CANCELED (отменён мерчантом)
```

### Полная таблица статусов

| Статус             | Описание                                               |
| ------------------ | ------------------------------------------------------ |
| `NEW`              | Платёж создан (после Init)                             |
| `FORM_SHOWED`      | Покупатель открыл платёжную форму                      |
| `DEADLINE_EXPIRED` | Истёк срок жизни ссылки                                |
| `CANCELED`         | Отменён                                                |
| `PREAUTHORIZING`   | Проверка данных карты                                  |
| `AUTHORIZING`      | Резервирование средств                                 |
| `AUTH_FAIL`        | Ошибка авторизации                                     |
| `AUTHORIZED`       | Средства зарезервированы (двухстадийный: ждёт Confirm) |
| `REJECTED`         | Отклонён банком эмитентом                              |
| `3DS_CHECKING`     | Проверка через 3-D Secure                              |
| `3DS_CHECKED`      | 3-D Secure пройден                                     |
| `REVERSING`        | Отмена резервирования                                  |
| `REVERSED`         | Резервирование отменено                                |
| `CONFIRMING`       | Подтверждение списания                                 |
| `CONFIRMED`        | **Деньги списаны — успех**                             |
| `REFUNDING`        | Идёт возврат                                           |
| `PARTIAL_REFUNDED` | Частичный возврат выполнен                             |
| `REFUNDED`         | Полный возврат выполнен                                |

---

## Какие webhook-события приходят при успешной оплате

**Одностадийный платёж (PayType=O):**  
Придут оба уведомления почти одновременно:

1. `Status=AUTHORIZED`
2. `Status=CONFIRMED`

Обновлять статус заказа следует на основании **`CONFIRMED`**.

**Двухстадийный платёж (PayType=T):**

1. `Status=AUTHORIZED` — деньги зарезервированы, ждём Confirm
2. `Status=CONFIRMED` — деньги списаны (после вашего вызова Confirm)

---

## Типы уведомлений (не только о платежах)

Т-Банк может присылать уведомления не только о статусах платежа:

- Привязка карты
- Фискализация (чек ОФД)
- Привязка счёта по QR-коду (СБП)

Идентифицировать тип можно по набору полей.

---

## Рекомендации по реализации для GetCourse / CRM

1. **URL вебхука** — уникальный на каждый заказ или общий с идентификатором в `NotificationURL` или в `DATA`.
2. **Идемпотентность** — одно уведомление может прийти несколько раз (повторные попытки). Перед обновлением статуса проверяйте текущий статус в CRM.
3. **Верифицировать токен** — обязательно, иначе любой может отправить поддельный вебхук.
4. **Логировать** — сохранять сырые тела всех входящих вебхуков для отладки.
5. **Всегда отвечать 200 OK** — даже если вы обнаружили дубликат или невалидный запрос.

---

## Ссылки

- [Уведомления об операциях (новый портал)](https://developer.tbank.ru/eacq/intro/developer/notification)
- [Нотификации (старый домен)](https://www.tinkoff.ru/kassa/develop/api/notifications/)
- [Подключение нотификаций — запрос](https://www.tinkoff.ru/kassa/develop/api/notifications/setup-request/)
- [Подключение нотификаций — ответ](https://www.tinkoff.ru/kassa/develop/api/notifications/setup-response/)
- [Вебхуки T-API](https://developer.tbank.ru/docs/intro/webhooks/)
