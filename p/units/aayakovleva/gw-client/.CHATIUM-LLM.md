# Проект gw-client (Payments Client)

## Что это

Универсальный клиент платёжных гейтвеев (многогейтвейная архитектура с 2026-05-28; ранее — `sbp-client`, моно-клиент LifePay). Одно Chatium-приложение совмещает: хранилище секретов магазинов LifePay, Lava.Top и GetCourse, серверную прокладку `POST /api/lp/invoke` (исторический URL) к выбранному upstream gateway, журналы в Heap, приёмники webhook от LifePay и Lava.Top. Upstream-гейтвеи: `p/saas/gw/lifepay` (контур `bills_v1`), `p/saas/gw/lavatop` (контур `invoices_v1`), `p/saas/gw/gc` (динамический каталог операций).

## Где искать детали

- Индекс каталога docs: `docs/README.md`
- Техническая информация: `README.md` (раздел «Текущее состояние (LifePay panel)»).
- Архитектура: `docs/architecture.md`
- API: `docs/api.md`
- Данные: `docs/data.md`
- Импорты/циклы: `docs/imports.md`
- Решения: `docs/ADR/` (вкл. `0003-internal-access-control.md` — проектный ADR 0003)
- История диалогов: `docs/LLM/`
- **Бизнес-логика виджета** (раскладка способов оплаты, правило 50 000 ₽): `docs/architecture/payment-scheme.md` (перенесено из ваулта 2026-05-24).
- **Проектный хаб** (бриф, реестр решений, data-flow, приёмка, база знаний сервисов LifePay/ОТП/Т-Банк): `../project-docs/README.md`.
- Gateway-SSOT (контракты `/v1/{op}`, контур `bills_v1`): `../../../saas/gw/lifepay/docs/gateway/operation-manual.md`.

## Ключевые инварианты (для LLM)

- **Многогейтвейность.** Тело `POST /api/lp/invoke` обязано содержать `gatewayId: 'lifepay' | 'lavatop' | 'gc'`. Без него — 400 `INVOKE_GATEWAY_REQUIRED`. Неизвестное значение — 400 `INVOKE_GATEWAY_UNKNOWN`. Для `gatewayId: 'gc'` дополнительно обязателен `httpMethod` (`'GET'` или `'POST'`); имя операции проходит `validateGcOpName` (SSRF-защита). Каталог LifePay/Lava.Top — статический (`shared/gatewayContract.ts`); каталог GC — динамический (`OPERATIONS_BY_GATEWAY.gc = []`, заполняется SSR-загрузчиком `lib/gateway/gcOperationsLoader.ts`). Операция, отсутствующая в каталоге — 400 `INVOKE_OP_UNKNOWN`.
- **UI-каталог операций.** Декларативное описание статических операций LifePay/Lava.Top в терминах формы — в `shared/operationsClientCatalog.ts` (`OPERATIONS_CLIENT_CATALOG`). GC-операции — динамические: тип `GcOperationEntry` с полем `argsTree?: ArgsTreeNode`; `argsTree` парсится из `GET /v1/operations` GC-гейтвея в `lib/gateway/gcOperationsLoader.ts`. Хелперы группировки/валидации/сборки `args` — в `shared/operationsClientForm.ts`; `groupOperationsForUi(gcOperations?)` принимает опциональный список GC-операций. При добавлении новой операции LifePay/Lava.Top — синхронно править каталог; для GC достаточно включить операцию на стороне `p/saas/gw/gc`.
- **Иерархическая форма GC.** Типы и хелперы (`buildFormRows`, `buildArgsObject`, `buildFieldErrors`, `jsonPlaceholder`, `FormRow`, `FormGroup`, `FormLeaf`, `LeafInput`) — кросс-проектный реэкспорт через `shared/gcArgsForm.ts` из `p/saas/gw/gc/shared/requestTestForm.ts`. Computed-блок и `prepareGcSubmit` — в `pages/sbpHomePageMixin.ts`; рендер иерархии — в `components/home/HomeGcRequestForm.vue`. Изменение схемы полей на стороне GC-гейтвея не требует правок клиента.
- **Диспатчер.** Точка входа диспатча — `lib/gateway/invokeDispatcher.ts → invokeByGateway(ctx, gatewayId, op, args, meta?)`. Выбирает клиент: `invokeLifepayGateway`, `invokeLavatopGateway` или `invokeGcGateway`. Для GC `meta.httpMethod` обязателен — диспетчер проверяет явно. Общие хелперы — в `lib/gateway/invokeResult.ts`.
- **Главная `/` — это панель оператора** (`pages/HomePage.vue`, компонент `ClientHomePage`), защищена `requireRealUser(ctx)` + `requireInternalAccess(ctx)` в `index.tsx`. Аноним → редирект на `/s/auth/signin`; авторизованный без гранта → `/web/forbidden` (403); Admin или активный грант → доступ. Старый `/web/panel` редиректит на `/` (легаси-совместимость).
- `guardInternalApi(ctx)` (= requireRealUser + requireInternalAccess) первой строкой всех `/api/lp/*`. Admin Chatium-аккаунта проходит `requireInternalAccess` всегда; не-Admin — только при активной записи в `panel_access` (ADR 0003, реализовано 2026-05-24).
- Доступ для не-Admin выдаётся через одноразовые пригласительные ссылки: `api/access/generate-invite` → `/web/access/invite?token=` → `api/access/consume-invite` (под `runWithExclusiveLock`). Переход по URL не расходует инвайт.
- Новые каталоги (ADR 0003): `lib/access/` (requireInternalAccess, invites, constants, apiGuard), `api/access/` (6 эндпоинтов), `web/access/invite/`, `web/forbidden/`, `shared/accessPages.tsx`, `pages/InviteAcceptPage.vue`.
- В журналах LifePay-операций (`request_log.argsRedacted`/`rawResponseBody`, `webhook_log.rawBody`/`rawQuery`/`email`) хранятся **сырые** payload — клиент является оператором персональных данных по 152-ФЗ. Структурная гигиена (циклы/несериализуемое/усечение > 64KB) — через `shared/prepareRawLog.prepareRawLog`. Маскирующие утилиты `shared/redact.ts` и `shared/redactRaw.ts` оставлены в коде с тестами, но в production-пути не используются.
- Webhook `/web/webhook` — анонимный, единственная аутентификация — токен в query. MD5-подпись webhook **не** проверяется (LifePay её не публикует).
- Серверные ретраи в прокладке **запрещены**: один входящий → один исходящий вызов к gateway.
- `requestId` берётся из заголовка `X-Gateway-Request-Id` ответа gateway, не генерируется заново.
- Секреты (`lp_apikey`, `lp_login`, `lp_webhook_token`) **не** попадают в `argsRedacted`, в `writeServerLog.payload`, в ответы аналитики и в журналы.
- В Vue (`.vue`) **запрещены** импорты `tables/`, `repos/`, `lib/` — данные только через SSR-пропсы и fetch к собственным API.
- Heap-схема не выражает unique constraint; дедупликация webhook — через `runWithExclusiveLock` из `@app/sync`.
- QR-код во вкладке «Создать запрос» — рендерится только если у выбранной операции в каталоге задан `paymentUrlPath` и в ответе по этому пути есть строка-URL (`LifePay.createBill`, `Lava.Top.createInvoice`). Используется CDN `cdnjs.cloudflare.com/ajax/libs/qrcode/1.5.1/qrcode.min.js` (UMD-сборка, выставляет `window.QRCode.toCanvas`); при недоступности — текст paymentUrl.

## Структура страниц

- `pages/HomePage.vue` (компонент `ClientHomePage`) — оркестратор главной (доступ — сотрудник + Admin через `requireRealUser` + `requireInternalAccess`). Подкомпоненты: `components/home/` (`HomeStatusStrip`, `HomeToolbar`, `HomeSearchResult`, `HomeOverviewTab`, `HomeRequestsTab`, `HomeWebhooksTab`, `HomeCreateRequestTab` — универсальная вкладка с дропдауном операций по гейтвеям, `HomeSettingsTab` + `HomeWidgetSettings` + `HomeWidgetOfferList` — operational/business-настройки (gc_enabled, виджеты), доступны сотрудникам, `HomeAccessTab` — Admin-only, `HomeRawModal`, `HomeCreateInviteModal`). Логика вынесена в `pages/sbpHomePageMixin.ts`; UI-каталог операций — `shared/operationsClientCatalog.ts` + хелперы `shared/operationsClientForm.ts`; форматтеры — в `shared/sbpHomeFormat.ts`. CSS — в `pagecss/sbpHomeCss1..4.ts`.
- `pages/AdminPage.vue` — оркестратор системной страницы настроек (`/web/admin`, **только Admin**). Подкомпоненты: `components/admin/` (`AdminCounters`, `AdminProjectSettings`, `AdminLogLevel`, `AdminLifePaySettings`, `AdminLavatopSettings`, `AdminGcSettings`). Только секреты, URL/host, инфра, логирование. Composable `shared/useLogsSocket.ts`. CSS — в `pagecss/sbpAdminCss1..4.ts`. SSR читает LP/Lava/GC настройки. Виджет-настройки и operational-флаги переехали на вкладку «Настройки» главной — там их редактируют сотрудники.
- `pages/TestsPage.vue` — оркестратор. Подкомпоненты: `components/tests/` (3 файла: `TestsToolbar`, `TestsMetrics`, `TestSuiteTab`). Composable `shared/useTestSuites.ts`, хелперы `shared/testSuiteHelpers.ts`. CSS — в `pagecss/sbpTestsCss1..3.ts`.
- CSS других компонентов: `pagecss/sbpProfileCss1.ts`, `pagecss/sbpHeaderCss1..2.ts`, `pagecss/sbpLogStreamCss1..2.ts`.

## Тесты

- Юнит: `lib/tests/lifepayUnitSuite.ts` (точка входа, re-export) + фазы `lifepayUnitPhase1Catalog..Phase5Correlation.ts`; `lib/tests/templateUnitSuite.ts` + `templateUnitSuiteHelpers.ts` + `templateUnitRoutesChecks.ts`.
- Интеграция: `lib/tests/integrationSuite.ts` (точка входа) + `integrationCoreSuite.ts`, `integrationApiSuite.ts`, `integrationE2eSuite.ts`, `integrationDateFilterSuite.ts`, `integrationAccessSuite.ts`; хелперы `integrationSuiteHelpers.ts`.
- Каталог тестов: `shared/testCatalog.ts` (типы + re-export) + `shared/testCatalogUnit.ts` + `shared/testCatalogIntegration.ts`.
- HTTP: вкладка «HTTP» в `/web/tests` (статус 200 и фрагменты SSR).

## Логирование

Без изменений из шаблона. Сервер: `lib/logger.lib.ts` → syslog RFC 5424. Клиент: `shared/logger`, `browserRemoteLogger` (пакетная отправка на POST /api/logger/browser). Уровень `log_level` (Debug/Info/Warn/Error/Disable). Секреты автоматически не попадают (`shouldIncludePayload` при level != Debug). PII в LifePay-журналах **не маскируется** — клиент-оператор, см. инвариант выше.

## Настройки lib

- `lib/settings.lib.ts` — геттеры, валидаторы, SETTING_KEYS, DEFAULTS, типы. Включает ключи GC (`gc_*`) и 12 виджет-ключей (`widget_lifepay_*`, `widget_lavatop_*`, `widget_offer_list_type`, `widget_offer_ids`, `widget_lavatop_offer_id`, `widget_lavatop_product_id`, `widget_lavatop_manual_rate_usd`, `widget_lavatop_manual_rate_eur`). Геттеры Lava.Top deal-потока: `getLavatopOfferId`, `getLavatopProductId`, `getLavatopManualRateUsd`, `getLavatopManualRateEur`. Валидатор `isValidManualRate`.
- `lib/widget/widgetSettings.lib.ts` — `getWidgetSettings(ctx)`: агрегатор 12 виджет-ключей через `Promise.all`; возвращает `WidgetSettingsData`.
- `lib/settings.mutations.ts` — `setSetting` (запись/сброс); re-export из `settings.lib.ts` сохраняет обратную совместимость импортов. Маскирует `gc_test_school_api_key` в exit-логе (наряду с `lava_test_apikey`, `lava_webhook_secret`).
- `lib/rates/currencyConverter.ts` — `convertRubTo(amount, currency)`: конвертация RUB → RUB/USD/EUR. `convertToRub(amount, currency)`: обратная конвертация валюты → рубли (умножение на курс); используется в `resolveGcDealAmount` для non-RUB заказов. Приватный `getRubForOne(currency)` — единый источник курса для обоих направлений: ручной курс из настроек → ЦБ РФ (HTTP-запрос к `cbr-xml-daily.ru`) → `RATE_UNAVAILABLE`.
- `lib/gateway/lavatopDealIntent.ts` — `handleLavatopDealIntent`: оркестратор Lava.Top deal-потока (фильтр → конвертация → лок → updateOfferPrice → createInvoice).

## Критические правила

- **Публичные виджеты.** Каталог `userscripts/` + `api/widgets/` (7 эндпоинтов). CORS — simple-request, `Content-Type: text/plain`. Hard-limit intent — 500 000 ₽ (`WIDGET_INTENT_HARD_LIMIT_RUB`, `shared/widgetSettingsTypes.ts`). CORS-проверка — `shared/widgetCorsCheck.ts`. Виджет-настройки в `SETTING_KEYS`: 8 базовых (`widget_lifepay_*`, `widget_lavatop_*` enabled/domains/max) + 4 Lava.Top deal-поток + 2 новых offer-ключа (`widget_lifepay_offers`, `widget_lavatop_offers`). Маршруты — `widgetConfig`, `widgetIntentLifepay`, `widgetIntentLavatop`, `widgetIntentByDeal`, `widgetSettingsGet`, `widgetSettingsSave`, `widgetOffers` в `config/routes.tsx`.
- **Двухуровневый offer-фильтр (реализовано 2026-06-01).** Фильтрация позиций заказа на двух уровнях: (1) клиентский DOM-фильтр в `userscripts/common.js` (`extractDealPositions` + `areAllPositionsAllowed`): читает `<li class="offer-position-{id}" data-offer-id="{id}">` + `.position-actual-title` из `.deal-positions`, виджет не показывается, если хотя бы одна позиция запрещена; (2) серверная проверка в `intent-by-deal` через `areAllOffersAllowed(positions, settings)` из `shared/widgetSettingsTypes.ts`: позиции берутся из `gcDealResolver.positions` (GC `getDealFields`), запрещённая → 403 `WIDGET_OFFER_NOT_ALLOWED`. Уровни независимы — сервер не доверяет клиенту. Семантика синхронизирована якорями «СИНХРОНИЗИРОВАНО». Нормализация: trim + схлопывание пробелов + lowercase. Сверка: по `id` ИЛИ нормализованному `title`. Дефолт `offerListType` — `'blacklist'`; режим «Выключен» = blacklist + [] (всё разрешено).
- **Offer-хранилище.** Новые ключи: `widget_lifepay_offers` / `widget_lavatop_offers` — JSON `AllowedOffer[]{id,title}`. Legacy: `widget_offer_ids` — только чтение (fallback). `parseAllowedOffers` в `shared/widgetSettingsTypes.ts` поддерживает оба формата. `data-offer-id` на якоре виджета удалён из сниппетов.
- **`/api/widgets/config` (с 2026-06-01)** — метод изменён с GET на **POST** (CORS simple-request, тело `{ dealId, positions }`). Ответ — `WidgetAvailabilityConfig { ok, config: { lifepay: {enabled}, lavatop: {enabled} } }` (не `WidgetPublicConfig`). Решение о `enabled` принимается на сервере: offer-фильтр + `resolveGcDealAmount` + `GcDealCache` (TTL=60с). **Проверка суммы ведётся в рублях**: `resolveGcDealAmount` конвертирует сумму заказа через `convertToRub` (RUB — без изменений; USD/EUR — ручной курс панели → ЦБ РФ; недоступен курс — fail-closed). Записи кэша удаляются при cache miss (lazy eviction). 400 `WIDGET_BODY_INVALID` при невалидном dealId при наличии minAmount/maxAmount.
- **Deal-поток (intent-by-deal).** `POST /api/widgets/intent-by-deal` — единый для LifePay и Lava.Top, ветвление по `method`. LifePay: только RUB. Lava.Top: RUB/USD/EUR, параметр `currency` обязателен. Сумма/валюта/email/positions из GC через `lib/gateway/gcDealResolver.ts` — `resolveGcDeal` (полный резолвер: getDealFields + getUserFields, возвращает сумму/валюту/email/positions). С 2026-06-01 `resolveGcDeal` возвращает `positions: {id,title}[]` — используется для `areAllOffersAllowed` перед платёжным потоком. **Не путать с `resolveGcDealAmount`** (лёгкий резолвер только суммы, без email/positions, используется только в `api/widgets/config`). `orderNumber`/`correlationId` = числовой dealId без префикса (нормализованный через `String(Number(...))`, например `"848988370"`). Email в логи не пишется (PII). `intent-lavatop.ts` (offer-поток) — deprecated. Домен GC-страницы должен быть в whitelist; `gc_enabled=true` обязательно. Коды ошибок: `WIDGET_GC_DEAL_ID_INVALID`, `WIDGET_GC_DEAL_NOT_FOUND`, `WIDGET_GC_ALREADY_PAID`, `WIDGET_GC_EMAIL_MISSING`, `WIDGET_GC_CURRENCY_UNSUPPORTED`, `WIDGET_GC_GATEWAY_ERROR`, `WIDGET_GC_METHOD_UNSUPPORTED`, **`WIDGET_OFFER_NOT_ALLOWED`**. Дополнительные Lava.Top: `WIDGET_LAVATOP_CURRENCY_INVALID`, `WIDGET_LAVATOP_NOT_CONFIGURED`, `WIDGET_LAVATOP_RATE_UNAVAILABLE`, `WIDGET_LAVATOP_PRICE_UPDATE_FAILED`, `WIDGET_LAVATOP_AMOUNT_TOO_SMALL`.
- **Lava.Top deal-поток.** Единый продукт (`widget_lavatop_offer_id` + `widget_lavatop_product_id`). `createInvoice` не принимает `amount` → поток: рублёвый `cost` GC → `areAllOffersAllowed` → `lib/rates/currencyConverter.convertRubTo` → `runWithExclusiveLock('lavatop-offer:'+offerId)` → `updateOfferPrice` → `createInvoice`. Лок защищает от гонок. Конвертация: ручной курс (`widget_lavatop_manual_rate_usd`/`eur`) приоритетнее курса ЦБ РФ. MIN_AMOUNT: RUB 10, USD 1, EUR 1.
- Не хардкодить URL: использовать `config/routes.tsx` (`PROJECT_ROOT = 'p/units/aayakovleva/gw-client'`, `withProjectRoot`, `withProjectRootAndSubroute`).
- В Vue (`*.vue`) запрещены импорты `tables/`, `repos/`, `lib/`, `pagecss/` — только `shared/*` с `// @shared`.
- CSS-файлы из `pagecss/` импортируются только в TSX-роутах через `<style>{...}</style>`.
- Документацию обновлять при изменениях.
- Перед коммитом — `/check`.
