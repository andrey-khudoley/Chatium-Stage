# 0017 — Копирование в p/saas/ref и проверка оркестратором

### Пользователь

Выполни инструкции из `p/template_project/docs/run.md` и скопируй проект в `/p/saas/ref/`. Это проект реферальной программы.

---

### Ассистент

Инструкции из run.md выполнены, проект реферальной программы создан в `p/saas/ref/`.

**Сделано:**
- Скопирован каталог `p/template_project` в `p/saas/ref/`.
- В `config/routes.tsx`: `PROJECT_ROOT = 'p/saas/ref'`.
- В `.dir.json`: `"name": "[INWORK] p/saas/ref"`.
- В `config/project.tsx`: название по умолчанию «Реферальная программа», подзаголовок «Приглашайте друзей и получайте бонусы».
- В `lib/settings.lib.ts`: в DEFAULTS — `project_name: 'Реферальная программа'`, `project_title: 'Рефералы'`.
- Отдельные Heap-таблицы: `tables/settings.table.ts` — `t__saas-ref__setting__7Fk2Qw`, `tables/logs.table.ts` — `t__saas-ref__log__9Xm3Kp`.
- В `docs/data.md` обновлено описание таблиц под новые ключи.

**Итог:** приложение доступно по адресу `https://<домен>/p/saas/ref/`. Дальше — коммит, пуш и при необходимости проверка оркестратором (@check).

---

### Пользователь

/check

---

### Ассистент

Выполнены проверки оркестратора по затронутым файлам проекта `p/saas/ref` (стандарты, file-based роутинг, рантайм и архитектура, импорты и циклы, документация, лог диалога).

**Сводка по категориям:**
- **Стандарты кода:** нарушений не найдено; затронутый код соответствует типичным правилам (отступы, кавычки, структура, ссылки через getFullUrl/ROUTES).
- **File-based роутинг:** замечаний нет; эндпоинты с путём `/`, ссылки через `getFullUrl(ROUTES.*)`.
- **Рантайм и архитектура:** критических рисков не выявлено; проверки прав (requireAccountRole, requireRealUser), асинхронность и граничные случаи учтены.
- **Импорты и циклы:** граф зависимостей по `docs/imports.md` проверен; циклических зависимостей не обнаружено.
- **Документация:** обновлены README.md (назначение и Changelog), .CHATIUM-LLM.md (название проекта), docs/architecture.md (назначение). docs/api.md и docs/data.md актуальны.

**Краткое резюме:** Код в порядке. Проект p/saas/ref — копия шаблона с обновлённым путём, названиями и отдельными Heap-таблицами; нарушений и критических замечаний нет. Документация приведена в соответствие с назначением «Реферальная программа».

**Что сделать дальше:**
1. Закоммитить и запушить изменения — после пуша Chatium задеплоит обновления.
2. Открыть приложение по адресу `https://<домен>/p/saas/ref/` и при необходимости задать название в админке.
3. После любых дальнейших правок снова запустить @check.
