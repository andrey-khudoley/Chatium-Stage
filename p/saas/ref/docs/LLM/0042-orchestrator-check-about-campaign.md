# 0042 — раздел «О компании», проверка оркестратором

Переписка: добавление раздела «О компании» на страницу кампании с возможностью удаления кампании; затем вызов @check и выполнение проверок оркестратором.

---

### Пользователь

На страничку кампании необходимо добавить ещё один раздел "О компании", где должна присутствовать возможность кампанию удалить

---

### Ассистент

**Сделано:**

- Создана страница AboutCampaignPage.vue: заголовок «О компании», описание, блок «Опасная зона» с кнопкой «Удалить кампанию», модальное подтверждение (ConfirmModal), вызов POST /api/campaigns/delete, при успехе — редирект на список кампаний; отображение ошибки API (в т.ч. «Удалять кампанию может только владелец»).
- В Sidebar добавлен пункт «О компании» (иконка fa-info-circle), ссылка на hash #about.
- В CampaignPageShell для секции #about подключён AboutCampaignPage с пропсами campaignId, campaignTitle, indexUrl.

**Итог:** Раздел «О компании» доступен из сайдбара кампании; удаление кампании доступно только владельцу (проверка на API).

---

### Пользователь

/s.chtm.aley.pro/check

---

### Ассистент

**Проверки оркестратора выполнены.**

**Стандарты кода:** Документ 001-standards.md в проекте ref не найден; проверка по типичным правилам Chatium: в AboutCampaignPage.vue — отступы 2 пробела, одинарные кавычки, относительные импорты (ConfirmModal), TailwindCSS-классы, FontAwesome (fas fa-trash-alt, fa-info-circle). Нарушений не выявлено.

**File-based роутинг:** Новых серверных роутов не добавлялось. Навигация по кампании — client-side hash (#about). Ссылки в Sidebar: campaignUrl + '#about'. Замечаний нет.

**Рантайм и архитектура:** AboutCampaignPage — проверка props.campaignId перед запросом; обработка ответа API (success/error); редирект через window.location.href только после успешного удаления; формирование href с учётом абсолютного/относительного indexUrl. Критических рисков не выявлено.

**Импорты и циклы:** В docs/imports.md добавлены AboutCampaignPage.vue (импорт vue, ConfirmModal) и обновлён CampaignPageShell.vue (импорт AboutCampaignPage). Циклов в графе зависимостей не обнаружено.

**Документация (docs-keeper):** Обновлены README (Changelog — раздел «О компании», пункт в «Текущее состояние» про #about и удаление кампании), docs/imports.md. API campaigns/delete уже описан в docs/api.md, изменений не потребовалось.

**Резюме:** Код в порядке. Критических замечаний нет. Документация приведена в соответствие.

**Что сделать дальше:** Дополнительных обязательных действий нет. После следующих изменений в коде повторить @check.
