# Проект `p/saas/gw/gc` — GetCourse Gateway

## Что это

Chatium-приложение: gateway к API GetCourse на каркасе `template_project` (страницы, админка, логи, тесты). Путь в воркспэйсе: **`p/saas/gw/gc`** (`PROJECT_ROOT` в `config/routes.tsx`).

## Норматив разработки gateway

Единый техдок слоя (маршруты `/v1/{op}`, заголовки, ошибки, маппинг на GC) — файл во Second Brain:

`/home/aley/obsidian/second-brain/03_Projects/active/course-chatium-gc-integration-3fa7c2/docs/specifications/gateway/gateway-operation-manual.md`

## Где искать детали в репозитории

- Обзор и деплой: `README.md`
- Архитектура: `docs/architecture.md`
- API: `docs/api.md`
- Данные (Heap): `docs/data.md`
- Решения: `docs/ADR/`
- История диалогов: `docs/LLM/`

## Тесты и логирование

Как в шаблоне: `shared/testCatalog.ts`, `lib/tests/templateUnitSuite.ts`, `lib/tests/integrationSuite.ts`, роуты `api/tests/*`; SSR `logLevel`, `browserRemoteLogger` на аутентифицированных страницах. См. README и `.CHATIUM-LLM.md` прежних правок в `docs/LLM/` при необходимости деталей UI.

## Критические правила

- Не хардкодить базовый путь приложения: `config/routes.tsx`, `withProjectRoot`.
- Клиентский бандл: не тянуть `lib/`, `repos/`, `tables/` из Vue — только `shared/` с `// @shared` и контракты `api/*/route`.
- Логирование в прикладном коде: через `lib/logger.lib` (`writeServerLog`), не `console.log`.
