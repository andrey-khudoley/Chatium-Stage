// @shared
export const lifepayProfileCss2 = `

.profile-field-label {
  color: var(--color-text-secondary);
  font-size: 0.9rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  font-weight: 500;
}

.profile-field-value {
  color: var(--color-text);
  font-size: 1.1rem;
  font-weight: 600;
  padding-left: 2.75rem;
}

@media (max-width: 768px) {
  .profile-heading {
    font-size: 2rem;
  }

  .profile-card {
    padding: 1.5rem;
  }

  .profile-field-value {
    padding-left: 0;
  }
}
`
