# Проект p/saas/gateways/gc_api

## Что это
Gateway GetCourse на Chatium: приложение `p/saas/gateways/gc_api` (на базе `template_project`). Публичный контракт **`/api/v1/invoke`** (Bearer по школе), **`/api/v1/operations`**, **`/health`**, онбординг и ротация токена. Heap: settings, logs, **gatewaySchool**, **opCatalog**, **requestLog**, **openapiCache**. Реестр операций — `shared/opRegistry.ts` (+ OpenAPI для контура new). Входящие webhook от GC на это приложение **не** настраиваются ([ADR-0003](docs/ADR/0003-gateway-outgoing-only.md)). Детали — `README.md`, `docs/architecture.md`, `docs/api.md`.

Страница тестов `/web/tests`: юнит и интеграция — `fetch(new URL('../api/tests/unit|integration', location.href))`. Роуты `api/tests/unit` и `integration` **без** `@shared-route`, иначе shared-бандл ломает `lib/crypto.lib` (Node `crypto`).

`lib/tests/templateUnitSuite.ts`: тестовые master key — base64-литералы (без `Buffer` на верхнем уровне модуля).

## Где искать детали
- Обзор и возможности шаблона: `README.md`
- Архитектура: `docs/architecture.md`
- API: `docs/api.md`
- Данные: `docs/data.md`
- Решения: `docs/ADR/`
- История диалогов: `docs/LLM/`

## Деплой
Изменения в репозитории → push → автодеплой Chatium. Локальный сервер не используется.
