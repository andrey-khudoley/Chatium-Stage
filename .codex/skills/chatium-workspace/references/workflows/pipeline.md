# /pipeline

Источник: `/home/aley/.cursor-server/data/User/globalStorage/chatium.chatium-sync/s.chtm.khudoley.pro/.claude/commands/pipeline.md`. Адаптировано для Codex.

Описание: Полный 12-шаговый конвейер разработки Chatium-задачи с обязательным делегированием субагентам.

Аргументы: `[формулировка задачи или ссылка на предыдущее обсуждение]`

Codex-адаптация:

- Slash-команды здесь не являются встроенными командами CLI; если пользователь пишет `/pipeline ` или явно называет workflow, воспринимай это как обычный запрос и следуй этому reference-файлу.
- Claude `Agent/subagent_type` заменяй на Codex `spawn_agent` только при явном разрешении на делегирование по правилам сессии.
- Shell-команды запускай через `exec_command`; правки файлов делай через `apply_patch`.

# /pipeline

Активируй **полный конвейер разработки** через субагента `pipeline-orchestrator`.

## Что произойдёт

Конвейер пройдёт 12 шагов:

1. Принять задачу
2. Формализация (`spawn_agent` -> `task-formalizer`)
3. План (`spawn_agent` -> `planner`)
4. Ревью плана (`spawn_agent` -> `plan-reviewer`)
5. Полнота ревью плана (`spawn_agent` -> `completeness-reviewer`)
6. Решение по плану (цикл 3→4→5 при правках)
7. Реализация кода (этот чат)
8. Ревью кода (`spawn_agent` -> `code-reviewer`)
9. Полнота ревью кода (`spawn_agent` -> `completeness-reviewer`)
10. Технические проверки (`spawn_agent` -> `verification-runner`)
11. Цикл исправлений (8→9→10 при критичных замечаниях, ≤ 3 итерации)
12. Финальный отчёт + опционально `docs-keeper`

## Инструкция запуска

Текущий Codex-чат выступает оркестратором. Открой `references/roles/pipeline-orchestrator.md` и выполняй его шаги.

Передай в оркестрацию:

1. **Формулировка задачи:** $ARGUMENTS (если пусто — возьми последнюю формулировку из чата).
2. **Полная переписка текущего чата** — она нужна оркестратору для контекста.
3. **Явное напоминание инвариантов:**
   - Шаги 2–5, 8–9, 10 — через `spawn_agent`, потому что `/pipeline` является явным delegated workflow.
   - Все 12 шагов обязательны; конвейер не считается завершённым без шага 12.
   - Цикл правок ≤ 3 итерации.

Не запускай отдельного `pipeline-orchestrator` как субагента без отдельной причины: это лишний слой. Делегируй именно специализированные этапы, а оркестрацию держи в текущем чате.

## Если конвейер прерывается на вопросах формализации

`task-formalizer` может вернуть уточняющие вопросы. Оркестратор передаст их пользователю и остановится. Дождись ответа пользователя в новом сообщении и продолжи конвейер с шага 2.
