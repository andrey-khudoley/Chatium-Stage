# TgChannelAnalytics

Аналитика переходов в телеграм-канал.

Проект в инфраструктуре Chatium.

## Структура проекта

```
channel/
├── index.tsx              # Главная страница (/)
├── login.tsx              # Страница входа (/login)
├── profile.tsx            # Страница профиля (/profile)
├── settings.tsx           # Страница настроек (/settings)
├── webhooks.tsx           # Страница вебхуков (/webhooks)
├── pages/                 # Vue компоненты страниц
│   ├── HomePage.vue       # Компонент главной страницы
│   ├── LoginPage.vue      # Компонент страницы входа
│   ├── ProfilePage.vue    # Компонент страницы профиля
│   ├── SettingsPage.vue   # Компонент страницы настроек
│   ├── BotsPage.vue       # Компонент страницы управления ботами
│   └── WebhooksPage.vue  # Компонент страницы вебхуков
├── api/                   # API endpoints
│   ├── auth-password.ts   # POST /password-hash - получение хеша пароля
│   ├── auth-telegram.ts   # POST /get-telegram-oauth-url - получение OAuth URL для Telegram
│   ├── bots.ts            # Управление ботами (GET /list, POST /add, POST /delete, POST /validate-token)
│   ├── settings.ts        # GET/POST /log-level - управление уровнем логирования
│   └── webhook.ts         # Обработка вебхуков от Telegram (POST /:id, GET /list, GET /check/:id, GET /test/:id)
├── sdk/                   # SDK функции для клиентской части
│   └── auth.ts            # Функции авторизации (SMS, Email, Password, Telegram)
├── tables/                # Определения таблиц
│   ├── settings.table.ts  # Настройки проекта (ключ-значение)
│   ├── bot-tokens.table.ts # Токены Telegram ботов
│   └── webhooks.table.ts  # Логирование входящих вебхуков от Telegram
├── shared/                # Общие модули
│   ├── debug.ts           # Класс Debug для структурированного логирования
│   └── Header.vue         # Общий компонент заголовка для страниц
├── lib/                   # Библиотеки и утилиты
│   └── logging.ts         # Управление уровнем логирования и кэширование
├── adr/                   # Architecture Decision Records
│   └── 000-template.md    # Шаблон ADR
├── tests/                 # Unit и интеграционные тесты
│   ├── index.tsx          # Интерактивная страница /tests
│   ├── ai.tsx             # AI страница /tests/ai (JSON API для автоматических тестов)
│   ├── pages/             # Vue компоненты для тестов
│   │   └── UnitTestsPage.vue
│   ├── api/               # API тесты
│   │   ├── run-tests.ts   # POST /run - запуск конкретного теста
│   │   └── start-tests.ts # POST /start-all, POST /start-single, GET /get-manual-socket-id
│   ├── jobs/              # Jobs для асинхронного выполнения тестов
│   │   └── test-runner.job.ts
│   └── shared/            # Общий код для тестов
│       └── test-definitions.ts
└── README.md              # Документация проекта
```

## Роуты

### HTML страницы

| Файл | Роут | Описание |
|------|------|----------|
| `index.tsx` | `/` | Главная страница (требует авторизации) |
| `index.tsx` | `/analytics` | Страница аналитики (требует авторизации, в разработке) |
| `index.tsx` | `/channels` | Страница управления каналами (требует авторизации, в разработке) |
| `index.tsx` | `/bots` | Страница управления ботами (требует авторизации) |
| `login.tsx` | `/login` | Страница входа |
| `profile.tsx` | `/profile` | Страница профиля пользователя (требует авторизации) |
| `settings.tsx` | `/settings` | Страница настроек (требует авторизации) |
| `webhooks.tsx` | `/webhooks` | Страница просмотра вебхуков от Telegram (требует авторизации) |

### API endpoints

| Файл | Метод | Путь | Описание |
|------|-------|------|----------|
| `api/auth-password.ts` | POST | `/password-hash` | Получение хеша пароля для авторизации |
| `api/auth-telegram.ts` | POST | `/get-telegram-oauth-url` | Получение OAuth URL для авторизации через Telegram |
| `api/bots.ts` | GET | `/list` | Получение списка ботов текущего пользователя |
| `api/bots.ts` | POST | `/add` | Добавление нового бота в таблицу (с автоматической регистрацией webhook в Telegram) |
| `api/bots.ts` | POST | `/delete` | Удаление бота из таблицы (с автоматическим удалением webhook в Telegram) |
| `api/bots.ts` | POST | `/validate-token` | Валидация токена Telegram бота |
| `api/settings.ts` | GET | `/log-level` | Получение текущего уровня логирования |
| `api/settings.ts` | POST | `/log-level` | Сохранение уровня логирования |
| `api/webhook.ts` | POST | `/:id` | Обработка входящих вебхуков от Telegram для конкретного бота (не требует авторизации) |
| `api/webhook.ts` | GET | `/list` | Получение последних вебхуков текущего пользователя (с опциональной фильтрацией по botId) |
| `api/webhook.ts` | GET | `/check/:id` | Проверка текущего webhook для бота в Telegram |
| `api/webhook.ts` | GET | `/test/:id` | Тестовый endpoint для проверки доступности webhook endpoint |

### Тесты

| Файл | Метод | Путь | Описание |
|------|-------|------|----------|
| `tests/index.tsx` | GET | `/tests` | Интерактивная страница для запуска тестов |
| `tests/ai.tsx` | GET | `/tests/ai` | JSON API для автоматических тестов |
| `tests/api/run-tests.ts` | POST | `/tests/api/run` | Запуск конкретного теста |

## Таблицы

### TgChannelAnalyticsSettings

Таблица настроек проекта в формате ключ-значение.

| Поле | Тип | Описание |
|------|-----|----------|
| key | String | Ключ настройки |
| value | Any | Значение (любой тип) |

### Настройки по умолчанию

| Ключ | Значение | Описание |
|------|----------|----------|
| project_title | Аналитика телеграм-каналов | Название проекта |
| log_level | info \| warn \| error | Уровень детализации логирования (по умолчанию: error) |

Для инициализации настроек по умолчанию используйте функцию `ensureDefaultSettings(ctx)` из `tables/settings.table.ts`.

### BotTokens

Таблица для хранения токенов Telegram ботов.

| Поле | Тип | Описание |
|------|-----|----------|
| token | String | Токен бота, введённый пользователем |
| botName | String | Название бота, полученное через API Telegram |
| botUsername | String | Имя бота (username с суффиксом "_bot") |
| userId | String | ID пользователя, который добавил бота |

Каждая запись привязана к пользователю через поле `userId`. При создании записи используйте `ctx.user.id` для привязки к текущему пользователю.

### TelegramWebhooks

Таблица для логирования всех входящих вебхуков от Telegram.

| Поле | Тип | Описание |
|------|-----|----------|
| botId | String | ID бота, которому принадлежит вебхук |
| userId | String | ID пользователя-владельца бота |
| updateId | Number (Optional) | Уникальный ID обновления от Telegram (для предотвращения дубликатов) |
| rawData | Any | Сырые данные вебхука в формате JSON |
| receivedAt | DateTime | Время получения вебхука |

Каждая запись привязана к боту через поле `botId` и к пользователю через поле `userId`. Вебхуки сохраняются автоматически при получении от Telegram через endpoint `POST /api/webhook/:id`.

### TelegramChats

Таблица для хранения уникальных телеграм-чатов и каналов, от которых хотя бы раз приходили вебхуки.

| Поле | Тип | Описание |
|------|-----|----------|
| chatId | String | Уникальный ID чата/канала из Telegram (используется как ключ для createOrUpdateBy) |
| botId | String | ID бота, от которого пришёл вебхук с этим чатом |
| userId | String | ID пользователя-владельца бота |
| chatType | String (Optional) | Тип чата (private, group, supergroup, channel) |
| chatTitle | String (Optional) | Название чата/канала (если доступно) |
| chatUsername | String (Optional) | Username чата/канала (если доступно) |
| firstSeenAt | DateTime | Время первого появления чата в вебхуках |
| lastSeenAt | DateTime | Время последнего появления чата в вебхуках |

Каждая запись привязана к боту через поле `botId` и к пользователю через поле `userId`. Чаты сохраняются автоматически при обработке вебхуков через endpoint `POST /api/webhook/:id` с использованием метода `createOrUpdateBy` для предотвращения дубликатов.

**Важно:**
- Если вебхук содержит информацию о чате (поле `chat`), чат автоматически сохраняется или обновляется в таблице
- Если вебхук не содержит информацию о чате, запись в таблицу не создаётся
- Для каждого уникального `chatId` создаётся одна запись, которая обновляется при последующих вебхуках от этого чата

## Работа с токенами ботов

### Добавление токена бота

Процесс добавления нового токена Telegram бота состоит из следующих этапов:

1. **Валидация токена** (`POST /api/bots/validate-token`):
   - Пользователь вводит токен в модальном окне на странице управления ботами
   - Токен отправляется на сервер для валидации через Telegram Bot API
   - Сервер делает запрос к `https://api.telegram.org/bot{token}/getMe`
   - Проверяется корректность токена и получение информации о боте (id, username, first_name)
   - При успешной валидации возвращается информация о боте

2. **Сохранение токена** (`POST /api/bots/add`):
   - Проверка авторизации пользователя (`requireRealUser`)
   - Валидация наличия токена в запросе
   - Проверка на дубликаты: поиск существующего токена у текущего пользователя
   - Если токен уже существует, возвращается ошибка "Этот токен уже добавлен"
   - Создание новой записи в таблице `BotTokens` с полями:
     - `token` - токен бота
     - `botName` - название бота (из Telegram API)
     - `botUsername` - имя бота (из Telegram API)
     - `userId` - ID текущего пользователя
   - Возврат информации о созданном боте

3. **Обновление интерфейса**:
   - После успешного добавления список ботов автоматически обновляется
   - Модальное окно закрывается
   - Новый бот отображается в списке

**Пример использования:**

```typescript
// 1. Валидация токена
const validationResult = await apiValidateTokenRoute.run(ctx, {
  token: '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz'
})

if (!validationResult.success) {
  // Обработка ошибки валидации
  return
}

// 2. Сохранение токена
const saveResult = await apiAddBotRoute.run(ctx, {
  token: '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz',
  botName: validationResult.botInfo.name,
  botUsername: validationResult.botInfo.username
})

if (!saveResult.success) {
  // Обработка ошибки сохранения
  return
}

// 3. Обновление списка
await loadBots()
```

### Удаление токена бота

Процесс удаления токена Telegram бота состоит из следующих этапов:

1. **Инициация удаления**:
   - Пользователь нажимает кнопку удаления (иконка корзины) на карточке бота
   - Вызывается функция `deleteToken(botId)` с ID бота

2. **Удаление через API** (`POST /api/bots/delete`):
   - Проверка авторизации пользователя (`requireRealUser`)
   - Валидация наличия `botId` в запросе
   - Поиск бота в таблице по ID
   - Проверка прав доступа: бот должен принадлежать текущему пользователю (`bot.userId === ctx.user.id`)
   - Если бот не найден, возвращается ошибка "Бот не найден"
   - Если бот принадлежит другому пользователю, возвращается ошибка "Нет доступа к этому боту"
   - Удаление записи из таблицы `BotTokens` через `BotTokens.delete(ctx, botId)`
   - Возврат успешного результата

3. **Обновление интерфейса**:
   - После успешного удаления список ботов автоматически обновляется
   - Удалённый бот исчезает из списка

**Пример использования:**

```typescript
// Удаление бота
const deleteResult = await apiDeleteBotRoute.run(ctx, {
  botId: 'bot_id_12345'
})

if (!deleteResult.success) {
  // Обработка ошибки удаления
  console.error('Ошибка при удалении:', deleteResult.error)
  return
}

// Обновление списка
await loadBots()
```

### Безопасность

При работе с токенами ботов реализованы следующие меры безопасности:

1. **Авторизация**: Все операции требуют авторизованного пользователя (`requireRealUser`)
2. **Проверка прав доступа**: Пользователь может удалять только своих ботов
3. **Валидация данных**: Проверка наличия обязательных полей (токен, ID бота)
4. **Проверка дубликатов**: Предотвращение добавления одинаковых токенов
5. **Логирование**: Все операции логируются для аудита и отладки

### Обработка ошибок

Возможные ошибки при работе с токенами:

**При добавлении:**
- `Токен обязателен` - не передан токен в запросе
- `Токен недействителен` - токен не прошёл валидацию через Telegram API
- `Этот токен уже добавлен` - токен уже существует у пользователя

**При удалении:**
- `ID бота обязателен` - не передан ID бота в запросе
- `Бот не найден` - бот с указанным ID не существует
- `Нет доступа к этому боту` - попытка удалить чужого бота

Все ошибки логируются через систему `Debug` с указанием контекста и stack trace.

## Работа с вебхуками

Проект поддерживает автоматическую обработку входящих вебхуков от Telegram для всех зарегистрированных ботов. Вебхуки сохраняются в таблицу `TelegramWebhooks` и отправляются владельцу бота через WebSocket в реальном времени.

### Регистрация webhook при добавлении бота

При добавлении нового бота через `POST /api/bots/add` автоматически выполняется:

1. **Создание записи бота** в таблице `BotTokens`
2. **Регистрация webhook в Telegram**:
   - Генерация URL webhook через `apiWebhookRoute({ id: botId }).url()`
   - Отправка запроса к `https://api.telegram.org/bot{token}/setWebhook` с параметром `url`
   - Логирование результата регистрации

### Обработка входящих вебхуков

Вебхуки от Telegram обрабатываются через endpoint `POST /api/webhook/:id`:

1. **Получение вебхука**:
   - Telegram отправляет POST запрос на `/api/webhook/:id`, где `:id` - это ID бота
   - Endpoint **не требует авторизации**, так как Telegram отправляет запросы напрямую

2. **Валидация бота**:
   - Поиск бота в таблице `BotTokens` по ID из URL параметра
   - Проверка наличия `userId` у бота (для определения владельца)

3. **Сохранение вебхука**:
   - Извлечение `update_id` из данных вебхука (если присутствует)
   - Создание записи в таблице `TelegramWebhooks` с полями:
     - `botId` - ID бота
     - `userId` - ID владельца бота
     - `updateId` - ID обновления (опционально)
     - `rawData` - сырые данные вебхука
     - `receivedAt` - время получения

4. **Отправка через WebSocket**:
   - Генерация `socketId` на основе `userId`: `webhooks-${bot.userId}`
   - Отправка данных через `sendDataToSocket()` с типом события `webhook`
   - Данные включают: `timestamp`, `botId`, `botName`, `botUsername`, `raw` (сырые данные)

5. **Ответ Telegram**:
   - Всегда возвращается `200 OK`, даже при ошибках
   - Это предотвращает повторные запросы от Telegram

### Просмотр вебхуков

Вебхуки можно просматривать на странице `/webhooks`:

- **Фильтрация по боту**: опциональный query параметр `?botId=...`
- **Real-time обновления**: новые вебхуки отображаются автоматически через WebSocket
- **API для получения списка**: `GET /api/webhook/list?limit=30&botId=...`

### Проверка webhook

Для проверки текущего состояния webhook в Telegram используется endpoint `GET /api/webhook/check/:id`:

- Запрос к `https://api.telegram.org/bot{token}/getWebhookInfo`
- Сравнение текущего URL в Telegram с ожидаемым URL
- Возврат информации о webhook и флага `isCorrect`

### Безопасность

При работе с вебхуками реализованы следующие меры безопасности:

1. **Изоляция по пользователям**: Вебхуки отправляются только владельцу бота через уникальный `socketId`
2. **Валидация бота**: Проверка существования бота и наличия `userId` перед обработкой
3. **Логирование**: Все операции логируются для аудита и отладки
4. **Обработка ошибок**: Все ошибки обрабатываются без прерывания работы, всегда возвращается `200 OK` для Telegram

### Примеры использования

#### Получение списка вебхуков:
```typescript
const result = await apiGetWebhooksListRoute.run(ctx, {
  limit: 30,
  botId: 'bot_id_12345' // опционально
})

if (result.success) {
  console.log(`Получено вебхуков: ${result.webhooks.length}`)
}
```

#### Проверка webhook:
```typescript
const checkResult = await apiCheckWebhookRoute.run(ctx, {
  id: 'bot_id_12345'
})

if (checkResult.success) {
  console.log(`Webhook установлен: ${checkResult.isCorrect}`)
  console.log(`Текущий URL: ${checkResult.webhookInfo.url}`)
  console.log(`Ожидаемый URL: ${checkResult.expectedUrl}`)
}
```

## SDK

Модуль `sdk/auth.ts` содержит функции для работы с авторизацией на клиентской стороне:

- `sendSmsCode(phone)` - отправка SMS кода
- `confirmSmsCode(phone, verificationCode)` - подтверждение SMS кода
- `sendEmailCode(email)` - отправка Email кода
- `confirmEmailCode(email, code)` - подтверждение Email кода
- `loginWithPassword(type, identifier, checkHashUrl, password)` - авторизация по паролю
- `handleAuthError(error)` - обработка ошибок авторизации
- `formatPhoneNumber(phone)` - форматирование номера телефона
- `isValidEmail(email)` - валидация email
- `isValidPhone(phone)` - валидация телефона

## Логирование

Проект использует централизованную систему логирования с настраиваемыми уровнями детализации. Все логи структурированы и содержат контекстную информацию для упрощения отладки.

### Архитектура

Система логирования состоит из следующих компонентов:

- **`shared/debug.ts`** - класс `Debug` для структурированного логирования
- **`lib/logging.ts`** - управление уровнем логирования и кэшированием
- **Таблица настроек** - хранение выбранного уровня логирования в `TgChannelAnalyticsSettings` с ключом `log_level`

### Уровни логирования

Поддерживаются три уровня логирования (от наиболее детального к наименее):

| Уровень | Описание | Когда использовать |
|---------|----------|-------------------|
| **info** | Информационные сообщения | Успешные операции, начало обработки запросов, количество найденных записей, обновление кэша |
| **warn** | Предупреждения | Отсутствие данных, некорректные параметры, проблемы с кэшем, использование значений по умолчанию |
| **error** | Ошибки выполнения | Ошибки базы данных, ошибки валидации, исключения с stack trace |

Дополнительно используется уровень **throw** для критических ошибок, которые прерывают выполнение программы.

### Настройка уровня логирования

Уровень логирования настраивается через страницу настроек (`/settings`):

1. Перейдите на страницу настроек
2. В разделе "Log Level" выберите нужный уровень:
   - **Info** - максимальная детализация (все логи)
   - **Warn** - только предупреждения и ошибки
   - **Error** - только ошибки (по умолчанию)
3. Нажмите "Сохранить"

Уровень логирования сохраняется в таблице `TgChannelAnalyticsSettings` с ключом `log_level` и кэшируется на 60 секунд для оптимизации производительности.

### Использование в коде

#### Серверная часть (API роуты, страницы)

```typescript
import { Debug } from '../shared/debug'
import { applyDebugLevel } from '../lib/logging'

export const apiGetBotsListRoute = app.get('/list', async (ctx, req) => {
  // 1. Применяем уровень логирования из настроек (обязательно в начале)
  await applyDebugLevel(ctx, 'api/bots/list')
  
  // 2. Логируем начало операции
  Debug.info(ctx, '[api/bots/list] Начало обработки запроса')
  
  try {
    // 3. Логируем успешные операции
    const bots = await BotTokens.findAll(ctx, { ... })
    Debug.info(ctx, `[api/bots/list] Найдено ботов: ${bots.length}`)
    
    // 4. Логируем предупреждения
    if (bots.length === 0) {
      Debug.warn(ctx, `[api/bots/list] У пользователя нет ботов`)
    }
    
    return { success: true, bots }
  } catch (error: any) {
    // 5. Логируем ошибки с stack trace
    Debug.error(ctx, `[api/bots/list] Ошибка: ${error.message}`, 'E_GET_BOTS_LIST')
    Debug.error(ctx, `[api/bots/list] Stack trace: ${error.stack || 'N/A'}`)
    return { success: false, error: error.message }
  }
})
```

#### Клиентская часть (Vue компоненты)

В Vue компонентах используется стандартный `console` для логирования:

```typescript
const loadBots = async () => {
  console.log('[BotsPage] Начало загрузки списка ботов')
  
  try {
    const result = await apiGetBotsListRoute.run(ctx)
    console.log('[BotsPage] Результат запроса:', result)
    
    if (result.success) {
      console.log(`[BotsPage] Получено ботов: ${result.bots.length}`)
    } else {
      console.warn('[BotsPage] Ошибка в ответе API:', result.error)
    }
  } catch (e: any) {
    console.error('[BotsPage] Исключение при загрузке:', e)
    console.error('[BotsPage] Stack trace:', e.stack)
  }
}
```

### Структура логов

Все логи имеют единообразную структуру:

```
[контекст] Сообщение: детали
```

**Примеры:**
- `[api/bots/list] Начало обработки запроса на получение списка ботов`
- `[api/bots/list] Пользователь авторизован: userId=12345`
- `[api/bots/list] Найдено ботов: 3`
- `[logging] Кэш уровня логирования обновлён: error -> info`
- `[settings] Инициализация настроек по умолчанию`

### Правила логирования

1. **Всегда применяйте уровень логирования в начале обработчика:**
   ```typescript
   await applyDebugLevel(ctx, 'контекст')
   ```

2. **Используйте префиксы контекста:**
   - `[api/bots/list]` - для API роутов
   - `[logging]` - для модуля логирования
   - `[settings]` - для работы с настройками
   - `[BotsPage]` - для Vue компонентов

3. **Логируйте ключевые этапы:**
   - Начало обработки запроса
   - Проверка авторизации
   - Успешные операции с результатами
   - Предупреждения
   - Ошибки с stack trace

4. **Для ошибок всегда указывайте:**
   - Код ошибки (если применимо)
   - Stack trace
   - Контекстную информацию

5. **Избегайте логирования чувствительных данных:**
   - Не логируйте пароли, токены, персональные данные
   - Используйте маскирование для токенов

### Файлы логирования

| Файл | Назначение |
|------|------------|
| `shared/debug.ts` | Класс `Debug` с методами `info()`, `warn()`, `error()`, `throw()` |
| `lib/logging.ts` | Управление уровнем логирования, кэширование, функции `applyDebugLevel()`, `persistLogLevel()`, `getCachedLogLevel()` |
| `tables/settings.table.ts` | Хранение уровня логирования в таблице настроек |
| `api/settings.ts` | API endpoints для получения и сохранения уровня логирования |
| `pages/SettingsPage.vue` | UI для выбора уровня логирования |

### Кэширование уровня логирования

Уровень логирования кэшируется на 60 секунд (TTL) для оптимизации производительности. При первом обращении к `applyDebugLevel()` уровень загружается из базы данных и сохраняется в памяти. Последующие вызовы в течение 60 секунд используют кэшированное значение.

При изменении уровня логирования через UI кэш обновляется немедленно.

### Примеры использования

#### Логирование успешной операции:
```typescript
Debug.info(ctx, `[api/bots/list] Найдено ботов: ${botsCount}`)
```

#### Логирование предупреждения:
```typescript
Debug.warn(ctx, `[api/auth-password] Отсутствуют обязательные параметры`)
```

#### Логирование ошибки:
```typescript
Debug.error(ctx, `[api/bots/list] Ошибка при получении списка ботов: ${error.message}`, 'E_GET_BOTS_LIST')
Debug.error(ctx, `[api/bots/list] Stack trace: ${error.stack || 'N/A'}`)
```

#### Критическая ошибка (прерывает выполнение):
```typescript
if (!isDebugLevel(level)) {
  Debug.error(ctx, `[logging] Недопустимый уровень логирования: ${level}`, 'E_LOG_LEVEL')
  throw Debug.throw(ctx, `Недопустимый уровень логирования: ${level}`, 'E_LOG_LEVEL')
}
```

## Тестирование

Проект использует систему unit и интеграционного тестирования с интерактивным UI и автоматическим JSON API для AI-агентов. Все тесты определены в едином источнике истины и выполняются асинхронно через Jobs с real-time обновлениями через WebSocket.

### Архитектура

Система тестирования состоит из следующих компонентов:

- **`tests/index.tsx`** - Интерактивная HTML страница `/tests` с Vue компонентом для ручного запуска тестов
- **`tests/ai.tsx`** - JSON API страница `/tests/ai` для автоматических тестов (используется AI-агентами)
- **`tests/api/run-tests.ts`** - Универсальный API endpoint `POST /tests/api/run` для выполнения конкретного теста
- **`tests/api/start-tests.ts`** - API endpoints для асинхронного запуска тестов через Jobs
- **`tests/shared/test-definitions.ts`** - Единый источник истины - список всех тестов и категорий
- **`tests/pages/UnitTestsPage.vue`** - Vue компонент с интерактивным UI для отображения и запуска тестов
- **`tests/jobs/test-runner.job.ts`** - Jobs для асинхронного выполнения тестов с отправкой обновлений через WebSocket

### Единый источник истины

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Все тесты определены в ОДНОМ месте - `tests/shared/test-definitions.ts`

Это гарантирует:
- ✅ Синхронизацию между `/tests` и `/tests/ai`
- ✅ Одинаковое количество и порядок тестов
- ✅ Простота обновления (один файл для изменения)
- ✅ Нет дублирования кода

### Категории тестов

Проект включает следующие категории тестов:

#### 1. Базовые тесты (`basic`)
- **`app_loads`** - Проверка загрузки приложения

#### 2. Тесты API endpoints (`api`)
Тестирование API через метод `route.run()` (серверные вызовы):
- **`validate_token_empty`** - Валидация токена: пустой токен
- **`validate_token_invalid`** - Валидация токена: невалидный токен
- **`add_bot_empty_token`** - Добавление бота: пустой токен
- **`add_bot_duplicate`** - Добавление бота: проверка дубликатов
- **`add_bot_success`** - Добавление бота: успешное создание
- **`delete_bot_empty_id`** - Удаление бота: пустой ID
- **`delete_bot_not_found`** - Удаление бота: бот не найден
- **`delete_bot_success`** - Удаление бота: успешное удаление

#### 3. Тесты HTTP доступности (`api_http`)
Проверка доступности endpoints через HTTP запросы:
- **`http_post_validate_token`** - HTTP POST `/api/bots/validate-token`
- **`http_post_add_bot`** - HTTP POST `/api/bots/add`
- **`http_get_list`** - HTTP GET `/api/bots/list`
- **`http_post_delete_bot`** - HTTP POST `/api/bots/delete`

#### 4. Тесты загрузки страниц (`pages`)
Проверка доступности HTML страниц:
- **`page_index`** - GET `/` - Главная страница
- **`page_login`** - GET `/login` - Страница входа
- **`page_profile`** - GET `/profile` - Страница профиля
- **`page_settings`** - GET `/settings` - Страница настроек
- **`page_channels`** - GET `/channels` - Страница каналов

#### 5. Тесты базы данных (`database`)
Тестирование работы с таблицей `BotTokens`:
- **`table_exists`** - Проверка существования таблицы BotTokens
- **`create_bot`** - Создание записи бота в таблице
- **`find_bot_by_user`** - Поиск ботов по userId
- **`find_bot_duplicate`** - Проверка поиска дубликатов токена
- **`delete_bot`** - Удаление записи бота из таблицы

### Интерактивная страница тестов (`/tests`)

Интерактивная страница для ручного запуска и просмотра результатов тестов.

#### Функционал

- **Запуск всех тестов** - кнопка "RUN ALL TESTS" запускает все тесты асинхронно
- **Запуск отдельного теста** - кнопка "[RUN]" рядом с каждым тестом запускает только его
- **Real-time обновления** - статусы тестов обновляются в реальном времени через WebSocket
- **Консоль вывода** - отображение логов выполнения тестов с временными метками
- **Статистика** - общее количество, пройдено, провалено
- **Визуальные индикаторы**:
  - `[ ]` - Ожидание (pending)
  - `[...]` - Выполняется (running)
  - `[OK]` - Пройден (passed)
  - `[ERROR]` - Провален (failed)

#### Использование

1. Перейдите на страницу `/tests`
2. Нажмите "RUN ALL TESTS" для запуска всех тестов или "[RUN]" для отдельного теста
3. Наблюдайте за выполнением в консоли вывода
4. Просматривайте результаты в списке тестов

### AI страница тестов (`/tests/ai`)

JSON API для автоматических тестов, используемый AI-агентами и системами мониторинга.

#### Формат ответа

```json
{
  "summary": {
    "total": 25,
    "passed": 23,
    "failed": 2
  },
  "results": [
    {
      "category": "api",
      "test": "validate_token_empty",
      "description": "Валидация токена: пустой токен",
      "success": true,
      "message": "Пустой токен корректно отклонён"
    },
    // ... остальные тесты
  ]
}
```

#### Использование

```bash
# Запуск всех тестов через curl
curl -X GET "https://s.chtm.aley.pro/saas/analytics/telegram/channel/tests/ai" \
  -H "Cookie: session=..."
```

### API endpoints

#### `POST /tests/api/run`

Запуск конкретного теста синхронно.

**Параметры:**
```json
{
  "category": "api",
  "test": "validate_token_empty"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Пустой токен корректно отклонён"
}
```

#### `POST /tests/api/start-all`

Запуск всех тестов асинхронно через Jobs.

**Ответ:**
```json
{
  "success": true,
  "testRunId": "test-run-1234567890-abc123",
  "encodedSocketId": "encoded_socket_id_here",
  "message": "Тесты запущены асинхронно"
}
```

#### `POST /tests/api/start-single`

Запуск одного теста асинхронно через Job.

**Параметры:**
```json
{
  "category": "api",
  "test": "validate_token_empty"
}
```

**Ответ:**
```json
{
  "success": true,
  "testRunId": "test-run-1234567890-abc123",
  "socketId": "tests-user-id-manual",
  "message": "Тест запущен асинхронно"
}
```

#### `GET /tests/api/get-manual-socket-id`

Получение `encodedSocketId` для предварительной установки подписки на WebSocket.

**Ответ:**
```json
{
  "success": true,
  "encodedSocketId": "encoded_socket_id_here",
  "socketId": "tests-user-id-manual"
}
```

### Real-time обновления через WebSocket

Система использует WebSocket для отправки обновлений о выполнении тестов в реальном времени.

#### Типы событий

- **`all-tests-started`** - Все тесты начали выполняться
- **`category-started`** - Начало выполнения категории тестов
- **`test-started`** - Начало выполнения конкретного теста
- **`test-completed`** - Завершение теста с результатом
- **`test-error`** - Ошибка при выполнении теста
- **`test-info`** - Информационное сообщение от теста
- **`category-completed`** - Завершение категории тестов
- **`all-tests-error`** - Критическая ошибка при запуске всех тестов

#### Пример события

```json
{
  "type": "test-completed",
  "data": {
    "category": "api",
    "testName": "validate_token_empty",
    "result": {
      "success": true,
      "message": "Пустой токен корректно отклонён"
    }
  }
}
```

### Асинхронное выполнение через Jobs

Все тесты выполняются асинхронно через Jobs для:
- Неблокирующего выполнения длительных тестов
- Возможности отправки обновлений через WebSocket
- Изоляции выполнения тестов от основного потока

Jobs находятся в `tests/jobs/test-runner.job.ts`:
- **`runAllTestsJob`** - Выполнение всех тестов
- **`runSingleTestJob`** - Выполнение одного теста

### Логирование в тестах

Все тесты используют систему логирования проекта:
- Применение уровня логирования через `applyDebugLevel()`
- Использование `Debug.info()`, `Debug.warn()`, `Debug.error()` для структурированного логирования
- Логи отправляются через WebSocket в реальном времени

### Очистка тестовых данных

Тесты, которые создают данные в базе (например, тестовых ботов), автоматически очищают их после выполнения:

```typescript
try {
  // Создание тестового бота
  const testBot = await BotTokens.create(ctx, { ... })
  
  // Выполнение теста
  // ...
} finally {
  // Гарантированная очистка
  if (testBot && testBot.id) {
    await BotTokens.delete(ctx, testBot.id)
  }
}
```

### Безопасность

- Все тесты требуют авторизации (`requireAnyUser`)
- Тесты изолированы по пользователям (используется `ctx.user.id`)
- Тестовые данные создаются только для текущего пользователя
- Чувствительные данные (токены) маскируются в логах

### Best Practices

1. **Тестируйте реальный код** - тесты должны вызывать существующие API routes и таблицы, а не создавать новую логику
2. **Используйте единый источник истины** - все тесты определяются в `test-definitions.ts`
3. **Очищайте тестовые данные** - всегда удаляйте созданные в тестах данные
4. **Логируйте ключевые этапы** - используйте `Debug.info()` для отслеживания выполнения
5. **Обрабатывайте ошибки** - используйте `Debug.error()` и `Debug.throw()` для критических ошибок
6. **Проверяйте авторизацию** - тесты, требующие пользователя, должны проверять `ctx.user`

### Пример добавления нового теста

1. Добавьте тест в `tests/shared/test-definitions.ts`:
```typescript
{
  name: 'api',
  title: 'Тесты API endpoints',
  tests: [
    // ... существующие тесты
    { name: 'new_test', description: 'Описание нового теста' }
  ]
}
```

2. Добавьте обработку в `tests/api/run-tests.ts`:
```typescript
async function runApiTest(ctx: any, testName: string) {
  switch (testName) {
    // ... существующие тесты
    case 'new_test':
      // Логика теста
      const result = await apiSomeRoute.run(ctx, { ... })
      if (!result.success) {
        Debug.throw(ctx, 'Тест провален', 'E_TEST_FAILED')
      }
      return { success: true, message: 'Тест пройден' }
  }
}
```

3. Тест автоматически появится на странице `/tests` и в `/tests/ai`

## История изменений

### 2025-01-XX
- Добавлена система централизованного логирования с настраиваемыми уровнями
- Реализован модуль `shared/debug.ts` для структурированного логирования
- Реализован модуль `lib/logging.ts` для управления уровнем логирования
- Добавлен UI для выбора уровня логирования на странице настроек
- Интегрировано подробное логирование во все API роуты и страницы
- Добавлено кэширование уровня логирования (TTL 60 секунд)
- Подключен вывод данных из таблицы `BotTokens` на страницу `BotsPage.vue`
- Все внутренние API вызовы используют метод `.run()` согласно документации
- Реализован функционал удаления токенов ботов:
  - Добавлен API endpoint `POST /api/bots/delete` для удаления бота из таблицы
  - Реализована проверка прав доступа (пользователь может удалять только своих ботов)
  - Обновлена функция `deleteToken` в `BotsPage.vue` для вызова API удаления
  - Добавлены тесты для удаления токенов (API, HTTP, база данных)
  - Добавлена документация процесса добавления и удаления токенов
- Реализована система обработки вебхуков от Telegram:
  - Добавлена таблица `TelegramWebhooks` для логирования входящих вебхуков
  - Реализован endpoint `POST /api/webhook/:id` для обработки входящих вебхуков (без авторизации)
  - Реализован endpoint `GET /api/webhook/list` для получения списка вебхуков с фильтрацией
  - Реализован endpoint `GET /api/webhook/check/:id` для проверки состояния webhook в Telegram
  - Реализован endpoint `GET /api/webhook/test/:id` для тестирования доступности endpoint
  - Добавлена автоматическая регистрация webhook при добавлении бота
  - Добавлена автоматическая отмена webhook при удалении бота
  - Реализована отправка вебхуков владельцу бота через WebSocket в реальном времени
  - Добавлена страница `/webhooks` для просмотра вебхуков с real-time обновлениями
  - Добавлен Vue компонент `WebhooksPage.vue` для отображения вебхуков
- Добавлены дополнительные страницы:
  - Страница `/analytics` (в разработке)
  - Страница `/channels` (в разработке)
  - Страница `/bots` для управления ботами
- Добавлен общий компонент `shared/Header.vue` для заголовка страниц

### 2025-11-27
- Создана базовая структура проекта
- Добавлена таблица настроек TgChannelAnalyticsSettings
- Добавлена дефолтная настройка project_title
- Добавлена таблица BotTokens для хранения токенов Telegram ботов

